# Project State — Affiliate Kit

Running list of wins and milestones. Most recent on top.

- **2026-05-24** — **Durable image + affiliate-tag safeguard infrastructure SHIPPED with auto-enforcement via Git pre-commit hook.** After Ray spotted broken product cards on a published DTP guide ("one perfect one, one is just a logo, one is a spray nozzle") and named the underlying ask ("we need to build into the systems things to avoid this. This should never have happened"), shipped two PowerShell lint scripts that catch the failure class at the repo edge: `scripts/lint-product-images.ps1` HEAD-fetches every `image:`/`hero:` URL in markdown frontmatter, validates 200 + image content-type + min-size + aspect ratio 0.35..2.5 (covers tall product bottles like Adam's Wheel Cleaner at 605×1500 = 0.40), decoding via `[System.Drawing.Image]::FromStream` after three failed attempts at a hand-rolled JPEG parser (Amazon CDN images carry EXIF thumbnail SOFs in the first ~5KB that fooled three successive segment-walker rewrites — final lesson: don't roll a JPEG parser when .NET ships one in-box); `scripts/lint-affiliate-tags.ps1` greps every `tag=<value>` across `.md` + `.astro` and verifies it matches each site's declared `affiliate.amazonTag`, skipping silently when a site hasn't wired its own tag yet (current DTP state). Both scripts use `-LiteralPath` so PowerShell doesn't choke on Astro's `[...slug].astro` routes whose filenames are wildcard-shaped. Wired into a Git pre-commit hook (`scripts/pre-commit-hook.sh` + `scripts/install-hooks.ps1`) so future commits with broken images or wrong tags fail at source instead of after deploy. Image lint caught **5 real broken DTP product images on first run** (a CE frontend audit yesterday missed all of them) — surfaced via Amazon product-page browse, swapped to working CDN renders Ray sourced directly: Gyeon Q²M Bathe, MTM Hydro PF22, Adam's Wheel Cleaner, P&S Brake Buster, CarPro Iron X. Side win: extracted `BottomLine.astro` to `packages/shared-ui/` per Ray's framing ("sites should be all using shared components so we wrtie ince use everywhere , like react") — the verdict-render now lives in one file instead of duplicated across MWC + DTP buying-guide templates. Root-cause investigation closed on the suffix-stripping hypothesis: no scaffolder strips Amazon `_AC_SL` suffixes — the real failure mode is Canopy `mainImageUrl` rotation over weeks (Amazon's "main" image pointer can flip to brand logos / wordmarks / variant thumbnails), making static scaffold-time URLs go stale. Lint catches staleness; long-term fix is build-time URL refresh or a Worker proxy. Commits: `dce3e3b` (shared BottomLine + image-CSS fix + 3 interior-cleaner image swaps), `8e426b3` (lint scripts + 5 image swaps + per-site `affiliate.amazonTag` config), `dd78693` (CarPro Iron X bumped to proper SL1500 render), `264a907` (pre-commit hook), `1a2b1ae` (MWC homepage hands-on claim removed + `we own` / `we've owned` added to voice doctrine after autonomous sweep caught it), `ce4e5eb` (Canopy-driven `audit-product-images.ps1` Layer-2 deep sweep + 13 image swaps to closer-to-square Canopy alternates + ReviewCard.astro CSS class-fix). Pre-commit hook **fired in production twice** (commits `1a2b1ae` and `ce4e5eb`) and ran both lints on staged content — proven working end-to-end, not just smoke-tested. Final safeguard stack: Layer 1 = auto-blocking pre-commit lint (catastrophic failures), Layer 2 = manual deep Canopy audit (composite-shaped images that pass Layer 1 but render poorly in 1:1 cards), Layer 3 = component-level CSS pattern (`max-width + max-height + object-fit: contain + width:auto/height:auto`) consistent across ReviewCard + qcard + deep-card.

- **2026-05-23** — **All 4 DetailerPicks buying guides Google-indexable + visual-identity layer shipped across both sites.** Biggest single SEO unlock since launch on the DTP side: last two DRAFTs (interior cleaner + wheel cleaner) closed with Bottom Lines written in Ray's voice (landscape + pick pattern matching the existing car-wash-soap voice anchor), `bottomLine.verdict.trim()` non-empty everywhere, page templates flip `<meta name="robots">` from `noindex,nofollow` to `index,follow` on next deploy. Same day: per-page gutter themes shipped on both sites — MWC's forest set (dawn / canopy / moss / twilight / pine / solid, mapped per content) and DTP's detailing set (foam-cascade / wheel-suds / suds-closeup / interior-detail / chrome-reflection / solid, per Ray's approved mockup at `docs/playgrounds/dtp-background/D-gutter-options.html`). Architecture: paint body bg via `<style is:inline>` in BaseLayout's head slot (Astro's `define:vars` couldn't reach body because vars cascade DOWN only), keep `<main>` transparent so the photo shows through 1100px-shell gutters either side. Five additional content moves: GardePro E5 replaces discontinued Vikeri in the trail-cam guide (`3b80cd6`), Chemical Guys Mr. Pink lands as the 6th wash-soap pick after catching Meguiar's Gold Class's wax-additive voice-doctrine conflict (`435d77c`), Adam's Polishes Wheel Cleaner lands as 6th wheel pick (`7848b40`), GardePro E5 standalone review SCAFFOLDED + WRITTEN + LIVE via the first end-to-end autonomous chain run (scout → research-product → scaffold-piece → stopped at Bottom Line for Ray's voice — proves the /aff pattern out), six zoomed-in DTP product images swapped for Canopy-canonical full-bottle shots (`1144eb0`, killed the older `/images/P/<ASIN>.01.LZZZZZZZ.jpg` pattern that returns aggressive crops). Bottom Line verdict now renders as two visually-distinct lines (primary forest semibold for the "Buy" sentence + counter muted ink for the "Skip" sentence) — sitewide treatment locked. Plus 6 follow-up debug commits to land the gutter rendering: real bugs in Astro's var-scoping (define:vars on component-root doesn't reach body), CSS-shorthand syntax (bare color cannot be its own layer), cascade ordering (inline head style vs compiled CSS rule), and finally the actual root cause — a `<main>` element painted cream by a leftover rule in MainLayout, plus a duplicate rule in DTP that the first fix missed (found via chrome-devtools `matchedCSSRules`). Lesson logged: at the first sign of a visual bug, inspect the live DOM via chrome-devtools before editing CSS source — would have saved 4-5 round-trips.

- **2026-05-20 (late)** — **`/aff` router + `/aff-idea` SHIPPED — single-front-door pattern adopted from moonlit-meadow's `/mm`.** After admiring the moonlit-meadow pattern (one state-aware command + one sidetrack capture, everything else hidden as internal mechanics), Ray asked for the affiliate-kit equivalent: *"the goal is for me to never have to type any commands other than aff , after that you ask , i answer in plain language , and YOU go do the things."* Wrote v1 proposal at `docs/brainstorms/2026-05-20-aff-router-proposal.md`, ran a 5-persona CE doc-review in parallel (`ce-product-lens`, `ce-scope-guardian`, `ce-feasibility`, `ce-design-lens`, `ce-coherence`), revised to v2 with all 5 corrections baked in, shipped as commit `fdd854a`. Surface: `/aff` (state-aware router across MWC + DTP) + `/aff-idea` (sidetrack capture, renamed from `/capture` to dodge moonlit-meadow's `/idea` global collision). The other 5 commands (`/scout-topics`, `/research-product`, `/scaffold-piece`, `/bottom-line-helper`, `/ops`) demoted to "Internal —" mechanics that `/aff` Read-and-execute inline (slash commands cannot invoke each other — feasibility verified). The state machine: scoped survey of MWC + DTP content/research/TODO state (satellites get one metric on demand only), 6-posture first-match-wins table, verbatim opener templates per posture leading with ONE move + Y/N (not a ranked menu — load shifts but doesn't disappear with menus), per-step Y/N gates on multi-step actions so file-system state stays durable across conversation crashes. Project CLAUDE.md adds the enforcement section. Key reviewer findings that shaped v2: (a) Claude Code has NO mechanism to suppress slash-command listing — "hidden" is description-prefix + social enforcement only; (b) `/mm` doesn't slash-invoke other commands, it Reads the playbook `.md` inline; (c) cut postures 10 → 6 (dropped satellite-rotation as contradicting dormant strategy, tooling/cf-deploy regressions as not-yet-wired integrations); (d) MWC + DTP full survey only, satellites at one metric on demand. Untested in the wild — first `/aff` invocation next session is the real test pass.

- **2026-05-20** — **mywildlifecam reaches DTP-grade polish bar — gutters back, editorial typography, image proportion uniformity, clickable review hero.** Five commits to production after a load-bearing recovery: an earlier same-day attempt at a photo-gutter theme system painted a photo on `<main>` and masked the center with `::before`, which broke every page rendered through `MainLayout.astro` (homepage whitespace blowup, review-card grid heights distorted, hundreds of px of empty cream on individual review article pages). Reverted via 4 revert commits (`91b1d6e` / `d3b4440` / `ba5d05c` / `6448139`), then a careful second pass shipped the same M1-locked solid-forest gutters via a different mechanism: paint the **body** forest (`#1F3D2F`) and wrap `<slot />` in a `.page-shell` (max-width 1320px, cream bg, soft drop shadow) outside the per-page section layouts. Sections render inside the shell at whatever width they want; nothing mid-tree opts in. Mobile (`<=760px`) collapses the shell to full-width because 12px gutters on a 375px screen read as a mistake. Same pass also: normalized `qcard` + `deep-card` product image sizes in buyers-guides (stripped the inline `transform: scale(imageScale)` that was making Spypoint visibly larger than Stealth Cam in the same row — "pic 2 can't be a close-up of the nozzle"); added editorial typography rhythm via 3 CSS patterns inside `.editorial .prose` / `.prose` / `.deep-card__body` (leading-bold lead-ins get serif punch-in, standalone-bold paragraphs render as pull-quote callouts, blockquote becomes a deliberate cream-card with a decorative quote mark); wrapped the review article hero image in `<a href={cloakedHref}>` with a forest "SEE ON AMAZON →" affordance pill (affects all 4 MWC reviews + future single-product reviews on satellites). All 5 page types verified at 1440px AND 375px before pushing — the bgTheme failure was caused by only verifying the homepage. Net commits: `95efdcc` (gutters), `2d21c7c` (image normalization), `fb38083` (typography), `1739034` (clickable hero), `73c6848` (session+todo docs). Load-bearing gotcha for future attempts at any layout-affecting change: paint outside the per-page sections (body or wrapper), not inside `<main>` with a pseudo-element mask. Closes TODO #19.

- **2026-05-19** — **Unified pick-card design system LOCKED across DetailerPicks; replication-ready for sitewide rollout.** Two-tier buying-guide pattern: "At a glance" 3-column quick cards (image + price badge + persona kicker + product-as-link + hook + CTA) + "The full detail" full-width expanded cards (image left + body middle + price rail right). Persona kickers are black bold sans (not brass mono caps). The ONLY blue/forest text is the product link itself — brass-soft underline accents it. Tables get ink-header + serif-bold first column + zebra stripes + hover. Editorial body goes full container width. Hero capped at 16:6 / 320px max-height (no more viewport-fillers). Schema additions: `hook` / `reason` / `facts` / `body` / `priceUnit` per product. All 4 DetailerPicks buying guides migrated to the new template (interior cleaner Phase A `7b8a0fd`, then wheel/wash/foam Phase B `0a04632`, polish passes through `e4a90e5`). Reference mockup at `docs/playgrounds/unified-pickcards/L-unified-system.html`. Ray's lockdown words: *"that my friend is our jam, we just essentially wash rinse repeat — color palette changes, some different pics and boom that is us bby."* Next: retrofit MyWildlifeCam (the original test site) with the same pattern; then the other 3 satellites.
- **2026-05-18 (late)** — **Detailerpicks redesigned + the Kit comprehensively documented.** Full Chrome & Suds palette landing on detailerpicks.com (charcoal+electric-blue+amber → off-cream+steel-blue+chrome+highlight, Space Grotesk+Inter → Instrument Serif+Inter Tight). Header bg dark→cream; brand mark blue-circle-SVG → italic-serif "Picks" inline; hero suds-over-chrome shot; manifesto section converted to inverted ink-dark contrast band (the page's one inversion). Article hero block added to buying-guide template rendering `images.hero` at 5:2 wide with soft dark gradient. Both buying guides got new article heroes (car wash soap → microfiber drag rag; foam cannon → placeholder pending Unsplash hunt). Verified live: `curl https://detailerpicks.com/` returns 200 with `F4F2EE` (new cream theme color) in served HTML. Old tokens preserved at `site-tokens.charcoal.bak.css` for revert. **`/scout-topics` slash command shipped** as 5th Kit command — discovery layer that sits before `/research-product`, two modes (no-flags surfaces 5-10 candidates; seed-flag like `--wheel-cleaning` returns 3-5 ranked angles via parallel Firecrawl + last30days). **PLAYBOOK.md rewritten** as the comprehensive operating guide Ray asked for — every slash command, every Claude Code skill the workflow uses (last30days, watch, brief, wrap, remember), every PowerShell script in `scripts/`, every API the system hits with key location and free-tier status, external accounts at-a-glance. 6 commits pushed today (45d193f, 4b6ac04, 61d9e23, e597dde, 101e0a5, 902c4c6).
- **2026-05-18** — **Affiliate Kit consolidated into one coherent system.** Eliminated the drift Ray was feeling: skills scattered between `~/.claude/commands/`, a stale "plugin" at `~/.claude/plugins/affiliate-kit/` that wasn't actually being loaded by Claude Code, and 4 overlapping plan docs in `docs/`. After today: source of truth for the 4 slash commands lives in `plugin/commands/` (in repo, version-controlled); `pnpm install-plugin` copies them into `~/.claude/commands/` where Claude Code actually reads them, and prints a setup checklist of external accounts + API keys for a fresh machine. New architecture doc at `docs/SYSTEM.md` is the single picture of how everything fits — this repo + Cloudflare + AIOS + Second Brain + external SaaS — with documented coupling points (AIOS reads this repo's KV; /capture writes to second-brain by hardcoded path) so future-Ray can find seams when something breaks. Stale plan docs moved to `docs/archive/` (Phase 1 design, Phase 1 plan, content-readiness plan, BASEMENT_SETUP). Project CLAUDE.md updated to point at SYSTEM.md as the canonical entry point. Dead `/aff-bootstrap` and `/aff-help` deleted (5 sites done, no 6th planned). Ray's framing: "I dont want one skill here and one skill there and the 'system' doesnt know about it." Fixed.
- **2026-05-17 (ultra-late)** — **Second Brain installed + CE strategy research locks in 2026 affiliate-site direction.** Forked Brad's `bradautomates/second-brain` template into private `champrt78/second-brain`, populated with full portfolio context (business profile, 5 project files including the AIOS workflow opportunities + 2026 SEO strategy + content tooling gaps + AI workflow platform comparison, 2 people files for Arrow contacts). Three parallel `ce-web-researcher` agents delivered critical 2026 signal: Google March 2026 core update hit affiliate-sites-without-hands-on at a 71% negative impact rate (Lars Lofgren's sites "obliterated"); Amazon Associates 180-day-3-sales clock unchanged with PERMANENT-rejection penalty for early application (action: don't apply until 10+ pieces live + warm social channel); Amazon commission rates niche-specific (mywildlifecam 3%, detailerpicks 4.5%, pets 3%) and stable since 2020; ShareASale → Awin migration complete end-2025 (open Awin account); Cloudflare Pages deprecated April 2025 in favor of Workers Static Assets (Q3-Q4 migration planning item); E-E-A-T methodology angle is DEFENSIBLE for the comparison-and-fit framework — Google requires transparency about experience type, not hands-on. Validates the existing content strategy as correctly positioned for post-update Google. Direct brand affiliate programs (Reconyx, Bushnell, Chemical Guys, Gyeon) pay 5-10% vs Amazon 3%. Canopy API has MCP support and is the highest-value next integration for Claude-stack operators. **Third buying-guide piece (detailerpicks Best Foam Cannon for Home Detailers) drafted and pushed** — three picks segmented by build attribute (MTM Hydro PF22 durability, Carbon Collective HD 314g weight, MJJC Pro V3 parts kit). DRAFT-gated. 5 new /watch transcripts processed including 3 affiliate-AI-tool videos that confirmed AIOS is the right workflow-orchestration choice for Ray's stack.
- **2026-05-17 (latest)** — **Two buying-guide-ready pieces drafted with verified specs + reviewer attribution.** Ray added ScrapeCreators, Brave, Exa, and Firecrawl API keys; Firecrawl CLI installed; the full search-scrape-watch pipeline now operational. Eight product pages scraped for verified spec data: CLEAN. Shampoo by Pan The Organizer ($18.99-$64.99, pH neutral, 1:500-1:256 dilution), Gyeon Q²M Bathe ($15.99, up to 500:1), Labocosmetica SÈMPER ($21.95, 1:800-1:1200, "free of waxes or gloss enhancers"), Phoenix E.O.D. Apex Auto Wash 3.0 ($17.99, no additives, veteran-owned), Pan Demineralizing Shampoo (acidic pH, every-few-months reset), plus full Tactacam Reveal X 3.0 manufacturer spec sheet from tactacam.zendesk.com. **detailerpicks piece #1 (Best Car Wash Soap for Home Detailers)** drafted at 125 lines with 5 picks mapped to 3 reader segments via the Dr. Beasley's "pure soap" vs Rag Company "ceramic-infused protective soap" doctrine split. **mywildlifecam piece #4 (Tactacam Reveal X 3.0 review)** drafted at 117 lines with 5-axis scorecard, full Buy-If/Skip-If, four disclosed honest cons, and a "How It Performs Over Time" section using a 4+ year owner-report transcript. Both pieces DRAFT-gated via Bottom Line placeholder so they noindex until Ray writes his Bottom Line. astro builds clean (9 pages detailerpicks, 13 pages mywildlifecam). Voice doctrine clean (no em dashes, no hands-on claims, no defensive exclusions). The two voice-doctrine-via-direct-quote constraint is documented in the session log.
- **2026-05-17 (very late)** — **Research pipeline shipped end-to-end + first real product-comparison data for both verticals.** Groq API key added to `~/.config/watch/.env` unlocking Whisper for any uncaptioned YouTube video; yt-dlp PATH wired in; 8 parallel `/watch` runs against trail-cam + detailing comparison videos produced source-attributed research notes at `docs/research/2026-05-17-{detailing,trail-cam}-research.md`. **Detailerpicks piece-#1 (Best Car Wash Soap)** is now scoping-ready with Pan The Organizer's full Jan-2026 tier list (S/A/B/C ranks for 10 products) plus 4-product direct slickness/wax-removal test (Clean by Pan overall favorite, Gyeon Bathe slickness king, Labocosmetica Sempre best value, CarPro Reset strips wax = bad for maintenance on coated cars). **Detailerpicks piece-#2 (Best Foam Cannon)** scoping-ready from 7-cannon comparison (MTM PF22.3 durability pick, Carbon Collective HD lightweight pick, MJJC Pro V3 spare-parts pick). **Mywildlifecam piece-#4 (Best Cellular Trail Camera by Use Case)** scoping-ready from full Tactacam Reveal lineup analysis (Defend for backyard wildlife counter-intuitively, Ultra for feature-complete, X 3.0 for budget) + cross-brand 6-cam Cell Cam Clash with Moultrie Mobile Edge 2 Pro's "got burned" caveat. Detailerpicks foundation also completed: shared `<ReviewArticle>` wired into reviews route, `/how-we-evaluate` methodology page shipped with Paint Safety 25% weighted axes. Confirmed empirically: zero-config `/last30days` = ~10% signal (1 useful Reddit thread per topic, rest is keyword bleed); ScrapeCreators is the remaining unlock for Reddit-comments owner-report depth.
- **2026-05-17 (late)** — **First AIOS workflow tied into the dashboard: `affiliate_link_health`.** Lives at `C:\Users\Ray\documents\github\AIOS\agents_system\workflows\affiliate_link_health\`. Walks the affiliate-sites monorepo, HEAD-requests every `/go/<slug>` cloaker URL plus every `images.hero` + `images.heroImages[]` + direct Amazon URL across all 5 sites, writes a markdown + JSON report, logs execution to AIOS SQLite so the dashboard at `http://localhost:8000/api/workflows/affiliate_link_health` reports run history. **First successful run caught a real bug** — `/go/vikeri-trail-camera-review` was returning 404 because the KV slug was never registered after today's piece-#3 add. Fixed via `pwsh scripts/add-link.ps1 -Site mywildlifecam -Slug vikeri-trail-camera-review ...`; re-run came back 18/18 healthy. This is the bug class the workflow was designed to catch and it shipped value on day one. SmallBrain analysis activates after 15 runs (currently 4). Manual invocation now; scheduled-run wiring (CronCreate / Windows Task Scheduler) deferred.
- **2026-05-17 (early)** — **mywildlifecam UI overhaul SHIPPED via `ce-brainstorm` → `ce-frontend-design`.** Site went from "5% done white-text-on-black" to a coherent elevated-nature-publication aesthetic in one pass. Direction locked in `docs/brainstorms/2026-05-17-ui-overhaul-requirements.md` (24 R-IDs): refined-outdoors register ("Land Rover not Jeep"), deep forest green (`#1F3D2F`) + warm cream (`#F5F0E6`) + brass (`#A07C3A`) palette, Fraunces variable serif + Inter Tight sans pairing. Shipped in commit `23628dd`: full design-token rewrite in `packages/shared-styles/src/tokens.css` (forest/cream/brass palette, editorial type scale, dark-mode kept warm); Google Fonts loading wired into `BaseLayout.astro`; sticky header with backdrop-blur-on-scroll + brand mark SVG + mobile hamburger drawer + multi-column forest-green footer in `sites/mywildlifecam/src/layouts/MainLayout.astro`; all 6 shared-ui components (`Hero`, `CTA`, `ReviewCard`, `BuyersGuideCard`, `AffiliateDisclosure`, `ComparisonTable`) refined for editorial restraint; new `ProductCard.astro` component for ranked buying-guide picks (R13/R14). All 9 mywildlifecam page templates rewritten with editorial layouts: full-bleed Unsplash pine-forest hero on home, 2-column body+sticky-spec-sidebar for single-product reviews, ranked ProductCard list for buying guides (fixed the "only 2 cams showing" bug by rendering all products explicitly from frontmatter, not depending on markdown body). Buyers-guide Zod schema extended with per-product `image`, `tagline`, `bestFor`, `priceFrom`, `cloakedSlug` + guide-level `images.hero` (both `templates/site-template/` + `sites/mywildlifecam/`). Best Trail Cameras content frontmatter updated with Spypoint + Stealth Cam manufacturer-CDN images + Unsplash hero. **Verified live on https://mywildlifecam.com/** after GH Actions run `25978968946` (5/5 sites deployed clean; smoke-test confirms `fonts.googleapis.com`, `--color-forest`, `brand__mark`, `hero--with-image` markers present in the served HTML). The 4 satellite sites (detailerpicks, fussybean, starteraquarium, gameovergear) inherit the new tokens automatically but keep their old layouts until their cycle turns — per-site brand differentiation deferred to the `sites/<slug>/src/data/site-config.json` layer that's scaffolded in the schema but not yet wired into layouts. Voice doctrine carries forward unchanged.
- **2026-05-16 (very late)** — **All 5 sites auto-deploy from `git push origin main` via GitHub Actions.** Replaced the per-site CF Pages dashboard-wiring approach with a single `.github/workflows/deploy.yml` matrix that builds + deploys all 5 sites in parallel using `npx wrangler pages deploy`. ~3 min from push to live across the portfolio. The cloudflare/wrangler-action@v3 had to be skipped (it tries `pnpm add wrangler@X` in the workspace root, which pnpm refuses) — direct `npx wrangler` via a `run` step works cleanly. Two cleanup deletions: the orphan `affkit-mywildlifecam` Pages project (no domain, replaced by the new `mywildlifecam` project) and the redundant `mywildlifecam` Workers-orchestrator (Ray's earlier-tonight Connect-to-Git project that was double-deploying alongside GH Actions). End state: 5 Pages projects total, one per site, no orphans, no double-deploys; `scripts/deploy.ps1` stays useful as a hotfix override; mywildlifecam.com owns the `mywildlifecam` Pages project while the 4 satellites still own `affkit-<slug>` projects until they migrate too (when, not gating). GH Actions secrets: `CLOUDFLARE_ACCOUNT_ID` (public) + `CLOUDFLARE_API_TOKEN` (Pages:Edit-scoped, set as a repo secret).
- **2026-05-16 (late)** — **mywildlifecam.com Git auto-deploy LIVE + content bones fixed.** mywildlifecam.com migrated from `affkit-mywildlifecam` (direct-upload Pages project) to a new `mywildlifecam` Workers-orchestrator + Pages project pair connected to GitHub. **`git push origin main` now auto-deploys mywildlifecam.com end-to-end in ~3 min** — first apex on the auto-deploy chain. Walkthrough at `docs/cf-pages-github-setup.md` documents the 6 friction points caught during the live attempt (CF docs say retrofit isn't supported, "Connect to Git" creates Workers project not Pages, default `pnpm run build` recursive build surfaced 2 pre-existing `tools/bootstrap` tsconfig bugs fixed in `e6e6c07`, "API token" field doesn't inject CLOUDFLARE_API_TOKEN reliably, `wrangler pages deploy` doesn't auto-create the target project, non-production branch deploy command can't be blank). Content "bones" fixed during the same session after Ray surfaced that the live site had broken Amazon links + no product images: real ASIN URLs (Spypoint Flex-M B0DKRHHQ5P, Stealth Cam DS4K Ultimate B09QBV2GP8) plus Wosports H-41 swapped to Vikeri Trail Camera (B09J546BBY) since Wosports H-41 has no Amazon listing → no Associates commission possible; hero images on the 2 single-product reviews (manufacturer CDN hotlinks per R13); link-cloaker Worker `/go/*` routes re-attached via `wrangler.toml` config-as-code (orphaned by the Pages migration, which is a worth-knowing failure mode for future site migrations). All 3 cloaked links verified 302-ing to real Amazon URLs. `scripts/deploy.ps1` updated with a `$projectNameMap` to handle the now-mixed naming (mywildlifecam → `mywildlifecam`, satellites still `affkit-<slug>`). 4 satellites still on manual `deploy.ps1`; GitHub Actions workflow queued as tomorrow's path to migrate them in bulk instead of repeating the CF dashboard friction 4 more times. UI polish (white-text-on-black bare aesthetic, no product cards, text wrapping issues) queued as a separate ce-brainstorm + ce-frontend-design pass for tomorrow.
- **2026-05-16 (later)** — **3 pieces SHIPPED on mywildlifecam — Amazon Associates application threshold hit.** Single arc validating the comparison-and-fit framework end-to-end. Piece #1 buying guide ("Best Trail Cameras for Backyard Wildlife"); pieces #2 and #3 are single-product reviews of the cellular pick (Spypoint Flex-M) and the image-quality pick (Stealth Cam DS4K Ultimate) from the buying guide's lineup. Cross-link architecture works in every direction: buying guide points to both reviews; reviews cross-link each other as cellular vs SD-card alternatives. Per the 2026-05-15 brainstorm, the Amazon Associates application was deferred until 2-3 pieces shipped to avoid burning the 180-day-3-sales clock on an empty site. That threshold is now hit; task #4 (Apply for Amazon Associates) is unblocked.

Ray's three Bottom Lines, captured for posterity (his voice, his framing): piece #1 "Don't overthink it, it shakes out like this:" + 3 numbered Pick X if Y picks; piece #2 "Buy it if you want convenience, skip it if you want gallery-quality photos." + 3-sentence synthesis; piece #3 "If photo quality is your goal, this is the move. Buy once, cry once. The trade-off is twelve AA batteries and a walk to swap the SD card." All voice-doctrine compliant; lint clean on all three. Three doctrine refinements landed during the arc: em dashes banned (AI tell), defensive audience exclusions removed (no "this isn't for X"), FAQ questions in second-person to avoid first-person-possessive lint hits. Full detail in `docs/sessions/Session_2026-05-16.md`. Piece #1: 3-product buying guide "Best Trail Cameras for Backyard Wildlife" at `/buyers-guides/best-trail-cameras-for-backyard-wildlife` (picks: Spypoint Flex-M, Stealth Cam DS4K Ultimate, Wosports H-41; Ray's Bottom Line is a "Don't overthink it" 3-numbered-list synthesis). Piece #2: single-product review "Spypoint Flex-M Review" at `/reviews/spypoint-flex-m-review` (deep-reviews the cellular pick from piece #1's lineup, creates the guide-to-review cross-link the architecture is designed for; Ray's Bottom Line is "Buy it if you want convenience, skip it if you want gallery-quality photos" plus 3-sentence synthesis). **All 3 pieces:** voice-doctrine compliant (lint clean), DRAFT gates cleared (`robots: index, follow`), live on the deployed site. Important caveat caught during verification: CF Pages was NOT wired to GitHub auto-deploy; the 2026-05-12 bootstrap created the projects via direct wrangler upload and never connected them to GitHub. Pieces had committed to `main` but stayed invisible on the live site until manually redeployed via the new `scripts/deploy.ps1`. All 5 sites manually redeployed 2026-05-16 evening (verified zero hands-on claims on About pages, all 3 piece Bottom Lines live). Permanent fix (CF Pages GitHub auto-deploy wiring) is now task #1 in `RAY_QUEUE.md` with walkthrough at `docs/cf-pages-github-setup.md`. **Doctrine evolved along the way:** em dashes banned (AI tell), defensive audience exclusions removed (no "this isn't for X" framing), FAQ questions should use second-person to avoid first-person-possessive lint hits. Pieces #1+#2 puts mywildlifecam at 2 live pieces, hitting the Amazon Associates application threshold (task #4 unblocks once piece #3 lands). Detail captured in `docs/sessions/Session_2026-05-16.md`.
- **2026-05-16** — **Comparison-and-fit content framework MVP SHIPPED via full CE-suite execution (ce-brainstorm → ce-plan → ce-work).** All 9 implementation units committed to main; all 3 workspace test suites green (26 + 6 + 16); all 5 sites build clean. The MVP delivers: voice doctrine v1 at `docs/voice-doctrine.md` (single source of truth for forbidden phrases, preferred framings, direct-question responses); universal `## Bottom Line` anatomy across both piece types (review + buyers-guide) with shared `DRAFT_MARKER` literal `"_The Bottom Line is being written._"`; `reviewSchema` JSON-LD emission dropped from review renderers (eliminates silent fabricated 5-star Reviews — research surfaced this contradiction during ce-plan Phase 1); scaffolders extended to emit a sibling `<slug>.prompt.md` artifact reading voice doctrine + per-site config; `scripts/lint-voice.ps1` back-stop greps generated bodies for forbidden literals from the doctrine (parses 36 literals from v1); per-site config at `sites/<slug>/src/data/site-config.json` carries reader segments, niche, brand tone, feature axes (mywildlifecam version locked: primary = homeowners/property/first-timer/gift; secondary = backpackers; excluded = hunters); all 5 forked About pages had "first-hand experience" / "We test products" / "My Take" claims removed (full positive-framed methodology block on mywildlifecam + template, stub paragraph on 4 satellites until cycle turns); CLAUDE.md content rules updated; PLAYBOOK.md gets a transitional banner pending post-MVP rewrite. 11 commits: pre-MVP cleanup (`b3c212d` + `1a63522`) + 9 unit commits (`f35e0da` U1 → `725c6ae` U3). Ray can now scaffold piece #1 via `pwsh scripts/new-review.ps1 -Site mywildlifecam ...` → paste prompt into Claude → write `## Bottom Line` → `pwsh scripts/lint-voice.ps1` → build → ship. Strategic question on askbigchew (Ray's bulldog site, where he IS hands-on) deferred to a future ce-brainstorm. Product DB / comparison-table renderer / satellite full About methodology / PLAYBOOK rewrite all explicitly deferred to F3 / pieces 2-5 per R15.
- **2026-05-15** — **Comparison-and-fit content framework SCOPED via `ce-brainstorm` end-to-end.** Strategic content pivot locked: Ray doesn't own and won't buy the products covered — sites were picked for affiliate-revenue potential, not personal interest. Existing toolchain (`## My Take` hard gate, "own it → review, don't own it → buyer's-guide" rule) assumes hands-on and is incompatible. Pivot direction: confident comparison-and-fit content that never claims hands-on use; spec-driven, use-case-framed, aggregated user reviews honestly; legally defensible via voice doctrine (no hands-on claims) + positive-framed methodology disclosure on About pages (documents research process without explicit "we don't test" disclaimer). Requirements doc at `docs/brainstorms/2026-05-15-content-framework-requirements.md` — Deep-feature scope, 15 R-IDs, 3 actors, 3 key flows, 5 AEs, 16-item scope-boundaries list, 9 key decisions, 4 outstanding questions deferred to planning. Key decisions: universal framework + per-niche tuning (not 6 separate frameworks); 2 piece types stay (single-product + buying-guide; head-to-head deferred); `## My Take` → `## Bottom Line` (hard DRAFT gate, moved to TOP of piece anatomy under an anti-recipe-page design principle) + `## Who This Is For` (structural, AI-drafted, not gated); shared per-site product database for verified specs (build during pieces 2-5); voice doctrine v1 + scaffolding scripts pull AI prompts from it; `docs/PLAYBOOK.md` to be rewritten; Amazon Associates application deferred until 2-3 pieces ship (180-day-clock management); MVP-first rollout (minimum changes for piece #1, full system grown across pieces 2-5). `ce-doc-review` queued autonomously against the requirements doc; results land in `docs/sessions/Session_2026-05-15.md` when complete. Next: address doc-review findings → `ce-plan` → `ce-work` to ship piece #1 under new framework.
- **2026-05-14** — **Content-production readiness SHIPPED via CE-suite end-to-end (ideate → plan → work).** Phase 1 left an "infrastructure built, content-production capability absent" gap; 4 parallel CE evaluation agents (tools/plugin, architecture/template, Worker, execution readiness) converged on the same finding. Plan written at `docs/2026-05-14-affiliate-kit-content-readiness-plan.md` (11 implementation units, Deep depth). Execution via parallel + serial subagent dispatch in 3 batches; 8 commits merged to `main`. Shipped: review + buyers-guide route renderers in template + back-propagated to all 5 sites (with JSON-LD wiring of `productSchema`/`reviewSchema`, DRAFT guard for empty `## My Take` that noindexes the page); `templates/review.md.tmpl` + `buyers-guide.md.tmpl`; `scripts/new-review.ps1` + `buyers-guide.ps1` (one-command scaffolding) + `scripts/add-link.ps1`/`list-links.ps1`/`remove-link.ps1` (wrangler KV wrappers with `--remote` + namespace ID baked in); `@astrojs/sitemap` integration (pinned to 3.4.1 for Astro 4.x compat); `BaseLayout.astro` SEO basics (robots, theme-color, og:image fallback, Twitter cards, canonical); silent `SITE_URL` SEO regression fixed (every site was canonicalizing to `https://example.com`); Worker URL collision fix (drop `<site>` segment from path, derive from Host header — closes cross-tenant link-leak); structured KV envelope (`{ url, tag, merchant, status, updated, replaced_by? }`) with `status=retired→410` and `replaced_by→301`; honest `COMMANDS.md` + auto-derived `/aff-help` slash command (reads `plugin/commands/` at invocation, prevents future doc drift); deleted unwired `tone`/`--spicy` config; `docs/PLAYBOOK.md` covering per-review workflow + 90-day cycle + refresh sweep + hero/satellite cadence. Pre-existing Phase 1 tooling gaps fixed along the way: vitest 1.6→2.1 (peer-dep mismatch was blocking Worker tests from ever running) + Analytics Engine binding stub in test config (pool v0.5 doesn't surface AE from `wrangler.toml`). Verification: bootstrap 6/6, shared-utils 26/26, Worker 16/16, all 5 sites build clean with sitemaps. Now: Ray can run `pwsh scripts/new-review.ps1 -Site mywildlifecam -Slug <slug> ...` and have a fully-scaffolded review with working cloaked link in one command.
- **2026-05-14** — **Show title LOCKED: `Starwatch Station: Stories of a Deep Space Trucker`.** Cross-repo ce-brainstorm + 4 parallel ce evaluation agents (name poetics, concept coherence, plugin audit, rename blast radius) ran against both `affiliate-sites` and the sibling `carillon-station` repo. Workshop confirmed the title — the `Starwatch Station Stories` triple `st-` alliteration is brand-load-bearing genre signal (Star Trek, Stranger Things, Stargate), not a tongue-twister bug. `Deep Space Trucker` → desired fandom contraction "DST." Adding `Deep` to the subtitle was the single highest-leverage word edit. Queued next: claim handles + register `starwatchstation.space` (Ray executing; pivoted from `.tv` to `.space` after price-shopping — `.space` was $2 first-year vs `.tv` at $26, and `.space` reads as in-universe canon for a sci-fi show set IN space; X variant pivots from `@StarwatchTV` to `@StarwatchSpace` to match); rename `carillon-station` repo + plugin prefix `/cs-` → `/show-`; flip spec tonal defaults from courier-warm to trucker-dry-wry; update this repo's `docs/launch-playbook.html` for the new brand.
- **2026-05-13** — **Semper Fi Studios social presence LAUNCHED.** All 7 Tier 1 social handles claimed tonight (YT `@semperfistudios1775`, Bluesky `semperfistudios.bsky.social`, IG `@semperfistudios1775`, X `@semperfi_studio`, TikTok profile+bio updated [username swap blocked until Jun 11 by 30-day cooldown], LinkedIn `/company/semper-fi-studios-co`, FB Page live). USPTO TESS scan clear via Trademarkia (no Live "Semper Fi Studios" mark in Class 042/035). Master Gmail 2FA verified locked down. Pivoted handle convention mid-flow from `@semperfistudios` to `@semperfistudios1775` (Marine Corps founding year) when IG forced the issue. namechk.com proved unreliable on Instagram — trust platform signup fields not third-party aggregators. Leaked gh token rotated (`gh auth switch --user champrt78 && gh auth refresh`).
- **2026-05-13** — **Carillon Station renamed to Starwatch Station** + character pivot from "retired courier" to "retired space trucker" (in-universe slang for cargo runners; Han Solo-coded). Workshopped 30+ candidates across 12 categories. Locked: `Starwatch Station: Stories of a Space Trucker`. Format = place-as-brand (Welcome to Night Vale style), subtitle telegraphs format. Availability confirmed clean on YouTube + Bluesky + TikTok + LinkedIn. .com is squatted (May 2024), pivot to `starwatchstation.tv` for the domain. Carillon was always placeholder per Ray; "carrion" sonic association was wrong for sleep brand.
- **2026-05-13** — **Brand launch playbook published** at `docs/launch-playbook.html` covering both Carillon Station + Semper Fi Studios. Single-file HTML with 12 step cards, per-platform handle priority logic, Cloudflare Email Routing click-by-click setup, and copy-paste-ready bios. Cross-repo authorship cleanup applied (filter-branch rewrote all commits to canonical champrt78 noreply email).
- **2026-05-12** — **Phase 1 complete: all 5 affiliate sites live on Cloudflare Pages** with the link-cloaker Worker (`affkit-link-cloaker`) deployed and routed at `<apex>/go/*` on each. mywildlifecam.com (hero), fussybean.com, detailerpicks.com, starteraquarium.com, gameovergear.games — all returning HTTP 200 via Cloudflare. KV namespace `AFFILIATE_LINKS` ready for affiliate slug→URL entries. Three real bootstrap CLI bugs found and patched along the way (Windows symlink copy, R2 graceful fallback, wrangler 4.x Pages API migration).
- **2026-05-12** — Locked the Phase 2 services-business brand: **Semper Fi Studios**. Purchased `semperfistudios.com` ($11, Porkbun). Marine-veteran-owned positioning, will cover both done-for-you affiliate site sales and local-business web design under one roof. SaaS products stay on a separate brand.
- **2026-05-12** — Phase 1 toolkit code complete (monorepo, shared packages, template, link-cloaker Worker, bootstrap CLI, Claude Code plugin). Ready for basement-PC bringup per `docs/BASEMENT_SETUP.md`.
