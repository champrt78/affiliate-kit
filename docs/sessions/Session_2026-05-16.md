# Session — 2026-05-16

## What We Did

- **Session-start `/brief`** — caught up on yesterday's content-framework brainstorm + 26-finding `ce-doc-review` results. Confirmed with Ray: 9 key decisions locked yesterday (universal framework + niche tuning, 2 piece types, Bottom Line at top, Bottom Line as hard DRAFT gate, shared per-site product DB, positive-framed methodology on About page only, MVP-first rollout, mywildlifecam-first, Amazon Associates deferred).
- **Decision: skip second `ce-doc-review` pass, apply 3 quick-fixes then go straight to `ce-plan`.** Rationale: 23 of the 26 findings need Ray's judgment on tradeoffs; another reviewer pass can't decide those — `ce-plan` forces the same decisions at the unit level where they're concrete.
- **Applied the 3 quick-fix findings** to `docs/brainstorms/2026-05-15-content-framework-requirements.md`:
  1. **R9 MVP carve-out** — R9 no longer contradicts R14; piece #1 explicitly exempted from product-DB requirement
  2. **R14 section-rename sync requirement** — listed all 7 files that must change together for the `## My Take` → `## Bottom Line` rename (2 `.md.tmpl` + 2 renderers in template + 5 forked site copies)
  3. **R14 hands-on disclaimer removal** — MVP scope now explicitly includes deleting the "we haven't tested this product ourselves" lines from `templates/buyers-guide.md.tmpl`
- **Ran `ce-plan` end-to-end** against the revised requirements doc. Three Phase-1 research agents dispatched in parallel (`ce-repo-research-analyst`, `ce-learnings-researcher`, `ce-spec-flow-analyzer`) and converged on **three load-bearing findings the brainstorm missed:**
  1. **File-sync surface is ~14 files, not 7.** Two different `DRAFT_MARKER` literals (reviews family vs buyers-guides family), 12 .astro renderers (2 types × 6 locations) + 2 .md.tmpl. R14's "7" was undercounting.
  2. **Renderer silently emits fabricated 5-star `reviewSchema` ratings** by default (`rating ?? 5` fallback at `[...slug].astro:51`). Bypasses the Bottom Line gate entirely. Direct R1 contradiction.
  3. **All 5 forked About pages claim "first-hand experience"** in live deployed copy. Keeping that while shipping piece #1 under new voice doctrine would collapse the legal-defense pillar on day one.
- **Surfaced the synthesis (Phase 5.1.5)** to Ray — 11 inferred plan-time decisions. Confirmed.
- **Plan written** at `docs/plans/2026-05-16-001-feat-comparison-fit-content-framework-mvp-plan.md`. Deep tier; 9 implementation units (U1 voice doctrine → U9 CLAUDE.md/PLAYBOOK transitional updates). Touches ~26 files across templates, 5 sites, scripts, docs. All 26 ce-doc-review findings addressed at the unit level OR explicitly listed in Scope Boundaries / Deferred to Follow-Up.

## Decisions

- **Skip `ce-doc-review` Round 2 on the brainstorm.** Move directly to `ce-plan` after the 3 quick-fixes. The remaining 23 findings get addressed at the unit level during planning.
- **AI-prompt wiring: sibling `<slug>.prompt.md` file.** Scaffolders emit a second file containing the constructed prompt; publisher pastes into Claude. Avoids API plumbing.
- **Voice doctrine home: `docs/voice-doctrine.md`** with H2 sections Forbidden phrases / Preferred framings / Direct-question responses. Plain markdown.
- **Per-site config format: JSON at `sites/<slug>/src/data/site-config.json`.** Avoids PowerShell/TS impedance.
- **Buyers-guide gate harmonized to `## Bottom Line`** (same as reviews — universal anatomy per R7).
- **Single shared `DRAFT_MARKER` literal** across both renderer families: `"_The Bottom Line is being written._"`.
- **Drop `reviewSchema` JSON-LD emission entirely.** Keep `productSchema`. Rating stays optional in Zod for future restoration. Eliminates default-5-star fabrication.
- **About-page sweep expanded to ALL 5 sites in MVP.** Hands-on disclaimer removal everywhere; full positive-framed methodology block on mywildlifecam + template only; satellites get a stub paragraph until cycle turns.
- **`scripts/lint-voice.ps1` ships with MVP.** Doctrine IS the legal defense; back-stop ships with v1.

## Discoveries / Gotchas

- Research-time discovery: **all 6 forked `about.astro` files currently say "products we have first-hand experience with. Our 'My Take' section reflects real use."** Wasn't in R14's MVP scope. Promoted to MVP scope to prevent doctrine bifurcation.
- PowerShell can't import TypeScript directly. Config artifact is JSON at `sites/<slug>/src/data/site-config.json` — both Astro and PowerShell can consume it cleanly.
- `reviewSchema` import in `packages/shared-utils` stays exported (future restoration); only call-sites stop invoking it. Don't delete the schema generator.

## Files Changed

- `docs/brainstorms/2026-05-15-content-framework-requirements.md` — R9 + R14 amendments (2 edits)
- `docs/plans/2026-05-16-001-feat-comparison-fit-content-framework-mvp-plan.md` — NEW (this plan)
- `docs/sessions/Session_2026-05-16.md` — NEW (this file)

## Status at End of Session — MVP SHIPPED

**All 9 MVP units committed to main.** Comparison-and-fit content framework MVP is live on the 5 Astro sites.

### Commit history (this session)

| Commit | Unit | Description |
|---|---|---|
| `b3c212d` | (pre-MVP) | Capture in-flight state — askbigchew #6 + 2026-05-15 brainstorm |
| `1a63522` | (pre-MVP) | Content framework MVP plan via ce-plan |
| `f35e0da` | U1 | Voice doctrine v1 (`docs/voice-doctrine.md`) |
| `868046e` | U5 | mywildlifecam per-site config |
| `86ccc08` | U9 | CLAUDE.md content rules + PLAYBOOK transitional banner |
| `24943f1` | U2 | Drop reviewSchema emission + `rating ?? 5` fallback removal |
| `872dca5` | U7 | `lint-voice.ps1` forbidden-phrase back-stop |
| `b6820ab` | U6 | Scaffolders emit sibling `<slug>.prompt.md` with voice doctrine + per-site config |
| `2603144` | U8 | About-page sweep across template + 5 sites |
| `406d6ff` | U4 | Buyers-guide anatomy + DRAFT marker harmonization + hands-on disclaimer removal |
| `725c6ae` | U3 | Review-piece anatomy + DRAFT marker harmonization |

### Verification

- `pnpm --filter @affkit/shared-utils test`: **26/26 ✓**
- `pnpm --filter @affkit/bootstrap test`: **6/6 ✓**
- `pnpm --filter link-cloaker test`: **16/16 ✓**
- `pnpm --filter "./sites/*" build`: **5/5 ✓** (7 pages + sitemap-index.xml each)
- grep verification: zero residuals of `## My Take` / `_Waiting for the human._` / `Editor's Note: Why this guide` / `replace with the actual editor's note` / `first-hand` / `We test products` across template + all 5 sites' source paths.

### Architecture deltas

- **JSON-LD truthfulness restored:** `reviewSchema` emission dropped from review renderers. No more silent fabricated 5-star Reviews.
- **Universal Bottom Line anatomy:** both piece types ship with `## Bottom Line` at TOP of anatomy, shared `DRAFT_MARKER` literal `"_The Bottom Line is being written._"` across both renderer families.
- **Voice doctrine is live infrastructure:** `docs/voice-doctrine.md` consumed by scaffolders (`scripts/new-review.ps1` + `scripts/buyers-guide.ps1`) at scaffold time AND by `scripts/lint-voice.ps1` at the back-stop check.
- **Per-site config infrastructure:** `sites/mywildlifecam/src/data/site-config.json` is the typed source of reader segments, niche, brand tone, and feature axes. Pattern established; satellite configs land per cycle turn (R15).
- **About-page consistency:** all 5 forked About pages no longer claim "first-hand experience"; full positive-framed methodology block lives on mywildlifecam + template.

### Execution pattern observations

- **Parallel subagent dispatch worked.** U4 + U6 + U7 + U8 ran simultaneously in the background (Agent tool, run_in_background=true) while U2 + U3 ran inline (same review-renderer files — sequential by necessity). 4 parallel agents returned within ~3 minutes. Save → parallel-batch-after-completion commit ordering with no merge conflicts.
- **No file collisions** between the parallel agents — file-to-unit mapping during plan-writing prevented overlap.
- **Verification pattern:** pre-sweep grep + post-sweep grep was the right call. Each unit's verification was a 2-line check, not a multi-minute test suite.

## What's next

1. **Scaffold piece #1** on mywildlifecam under the new framework: `pwsh scripts/new-review.ps1 -Site mywildlifecam -Slug <product-slug> -ProductName "..." -Brand "..." -AmazonUrl "https://amzn.to/..."`. This is the live validation of the MVP.
2. Paste the generated `<slug>.prompt.md` into Claude; review the draft against the voice doctrine; write the `## Bottom Line` section.
3. Run `pwsh scripts/lint-voice.ps1 sites/mywildlifecam/src/content/reviews/<slug>.md` as the back-stop check.
4. `pnpm --filter mywildlifecam build` to verify DRAFT gate behavior; commit; push; Cloudflare Pages deploys.
5. (Deferred to future sessions) askbigchew strategy brainstorm — migrate vs stay separate; voice doctrine variants for hands-on bulldog content; tier reshuffle. NOT a blocker for piece #1.
6. (Deferred) Product database schema + comparison-table renderer + F2 trigger rule + verified-date staleness windows — these grow with pieces 2-5 per R15.

---

## Post-MVP burst — push, smoke-test, trackers, piece #1 DRAFT

Ray asked "any small wins we can knock out?" after the MVP wrap. Picked: push to remote + smoke-test scaffolder + tracker refresh. Then he came back later and we kicked off piece #1.

### What shipped

| Commit | What |
|---|---|
| `aeab2ec` | Session log + PROJECT_STATE wrap for the 9-unit MVP (first push to remote) |
| `927cc54` | Trackers refresh: RAY_QUEUE.md + tasks.json — 3 content-framework tasks flipped to done; "scaffold piece #1" promoted to top of queue; new `askbigchew-strategy` task captured |
| `097ccb5` | **fix(scripts): U6 follow-up — relocate prompt files outside src/content/.** Real bug: U6 scaffolders wrote sibling `.prompt.md` files into `src/content/{reviews,buyers-guides}/` where Astro's content collection picked them up and Zod-validated them as buying-guide entries. Build-blocking. Caught during the first attempt to scaffold a real piece. Fixed by relocating to `sites/<slug>/prompts/<slug>.prompt.md`. |
| `045eb47` | **feat(content): piece #1 DRAFT** — `best-trail-cameras-for-backyard-wildlife.md` buying guide for mywildlifecam |

### Smoke test (pre-piece-#1)

Ran `pwsh scripts/new-review.ps1` with a fake `test-smoke-spypoint-flexm` slug. Confirmed both files produced (the `.md` scaffold + the `.prompt.md` sibling). Inspected the prompt — it had all expected sections (site context, full voice doctrine forbidden phrases, preferred framings, embedded scaffold). Lint script ran clean. Test files cleaned up. This was the first end-to-end validation of the pipeline.

### Piece #1 details

- **Topic:** "Best Trail Cameras for Backyard Wildlife (2026): Spec-Based Picks"
- **Picks (3 — trimmed from 4 mid-flight on Ray's request):**
  - Spypoint Flex-M (cellular convenience)
  - Stealth Cam DS4K Ultimate (SD-card image quality without subscription)
  - Wosports H7 (gift / first-time / under-$80 entry)
- **Excluded (per R12):** hunter-targeted content. Explicit "Hunters scouting deer" exclusion in the Who Should Skip section.
- **Voice-doctrine compliance:** `scripts/lint-voice.ps1` ran clean. No first-person hands-on claims. Spec attribution to manufacturer listings and "verified-buyer reviews on Amazon" patterns. Conditional comparisons throughout.
- **Status: DRAFT.** `## Bottom Line` placeholder unfilled. Page renders with DRAFT banner + `<meta name="robots" content="noindex,nofollow">`. Verified in dist output.
- **HUMAN TODO block at top** flags: write Bottom Line; verify spec claims against current Amazon listings; swap affiliate URLs after Associates approval; run lint before commit.

### Bug worth remembering

The U6 scaffolders' original prompt-file location (`src/content/{reviews,buyers-guides}/<slug>.prompt.md`) was a real architectural mistake — Astro picks up any `.md` in those directories as collection entries. The U6 agent's smoke-test had cleaned up its test files before any `astro build` ran, so the bug never surfaced during the MVP execution. It bit on the very first real piece. Lesson: **smoke-tests that delete artifacts before running the full build pipeline can miss build-time bugs.** Going forward, smoke-tests should run `astro build` against the scaffolded output before declaring done.

### Stopping point

- Working tree clean, all commits pushed to `origin/main` (17 commits this session total).
- Piece #1 ready for Ray's `## Bottom Line` writing (2-4 sentences, his voice).
- Build verified passing (8 pages incl. new buying guide).
- Spec claims need Ray's pre-publish verification against Amazon listings.

---

## Late addendum — Session/PROJECT_STATE discipline reset (NON-NEGOTIABLE rule codified)

Ray asked "have you been updating the session and project state file?" — knowing the answer would be no. Honest accounting: I'd written a strong end-of-MVP wrap entry, then drifted through 3 more commits (trackers refresh, U6 bug fix, piece #1 DRAFT) without touching either file. Caught up after his prompt (commit `5a5ad26`) but the lapse was the point.

Ray's reinforcement: *"that is non negotiable, that is how i do everything and ensure context between sessions, sometimes the memory file isnt enough, this is so I can remember and you and we dont fall for the same gotchas or duplicate efforts, you behind the scenes write down things i am not aware of so like weird dev related things"* and *"keeps it honest and real"*.

**Reframed understanding (this matters for every session going forward):**

1. **Session log + PROJECT_STATE is Ray's accountability mechanism, not just an AI handoff convenience.** The act of reading the narrative is how Ray retains context, prevents drift, prevents duplicate efforts, prevents repeating gotchas.
2. **"Weird dev things behind the scenes" is an explicit deliverable.** When subtle technical details surface during execution (build-time gotchas, schema constraints, tool oddities, unexpected dependencies) — Ray often won't have watched that happen because it was a subagent's output or mid-edit verification. Surfacing those to him via the session log is part of the job.
3. **Memory files are not sufficient.** Memory is for indexed facts; session log is for narrative. Both matter, neither replaces the other.

**Actions taken:**

- Strengthened `~/.claude/CLAUDE.md` "Session Documentation" section with the three-audience framing, NON-NEGOTIABLE label, and Ray's exact words preserved as canonical rationale.
- Added new "Session Documentation" section at the top of project-local `CLAUDE.md` mirroring the rule (visible in-repo for anyone reading project conventions).
- Wrote new feedback memory at `~/.claude/projects/.../memory/feedback_session_state_proactive.md` with full rationale + concrete application bar.
- Updated `MEMORY.md` index to include the new feedback memory.

**Pattern to remember:** A commit lands → session log gets a bullet in "What We Did" with the hash, immediately. No batching. If PROJECT_STATE deserves a sub-day entry for the work, append it. The cost of an extra `Edit` call is ~5 tokens; the cost of losing context is the whole reason these files exist.

---

## Late addendum #2 — Session log vs PROJECT_STATE bar distinction

Ray clarified after the rule-codification commit: *"session file helps us both just remember what we did the day before, project state is the overall state of the project, the big wins and milestones."*

I had been conflating the two bars. The "2026-05-16 (evening)" entry I appended to PROJECT_STATE earlier today was overstuffed — included trackers refresh, U6 bug fix, smoke-test details. Those belong in the session log, not in PROJECT_STATE. The actual milestone-level fact is just "first piece scaffolded as DRAFT — validates MVP on real content." Trimmed the PROJECT_STATE entry accordingly.

**Distinction codified in:**
- `~/.claude/CLAUDE.md` (global) — Session Documentation section now leads with the two-files-two-bars framing
- `affiliate-sites/CLAUDE.md` (project) — same fix, in-repo visibility
- `feedback_session_state_proactive.md` (memory) — full nuance with Ray's exact words preserved

**Test for PROJECT_STATE-worthiness:** would you tell someone "today we shipped X" in a status update? If yes, PROJECT_STATE-worthy. If it's "we made progress and fixed a bug along the way," session-log-only.

## Next session opening

Reading this session log + `git log --oneline -20` should fully orient. To pick up piece #1:

1. Open `sites/mywildlifecam/src/content/buyers-guides/best-trail-cameras-for-backyard-wildlife.md`
2. Write the `## Bottom Line` section (2-4 sentences in Ray's voice)
3. Run `pwsh scripts/lint-voice.ps1 sites/mywildlifecam/src/content/buyers-guides/best-trail-cameras-for-backyard-wildlife.md`
4. `pnpm --filter mywildlifecam build` to verify the DRAFT banner is GONE
5. Commit + push

The plan file at `docs/plans/2026-05-16-001-feat-comparison-fit-content-framework-mvp-plan.md` carries the architectural decisions if any unit needs revisiting.

## Next Steps

1. Ray confirms execution path (ce-work / Issue / pause).
2. If `ce-work`: execute units U1-U9 in dep order (U1, U2, U3, U4, U5 parallel-safe; U6 depends on U1+U5; U7 depends on U1; U8, U9 independent).
3. After unit landings: `pnpm -r build` across all 5 sites; `pnpm test` in 3 workspaces (shared-utils, bootstrap, link-cloaker).
4. Scaffold piece #1 on mywildlifecam under the new framework as the live MVP validation.
