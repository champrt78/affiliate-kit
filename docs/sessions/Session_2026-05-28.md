# Session — 2026-05-28

## What We Did

- Ray dropped `2026-05-28-steal-from-aaron.md` (drafted on his work PC during an email thread with Aaron Champion 10:50 AM–1:50 PM today) onto his Desktop and handed it to me on the home PC. The doc codifies a 6-item Magic Go vision — single autonomous overnight run produces N DRAFT pieces, Ray writes Bottom Lines in the morning, single `/aff publish-batch` flips DRAFT→published.
- Copied the plan into the repo at `docs/brainstorms/2026-05-28-magic-go-vision.md` so it's version-controlled and survives if the Desktop copy gets lost.
- Refreshed `docs/TODO.md`: added the Magic Go North Star at the top, reorganized into Now/Next/Later by the doc's prerequisite chain (#1 link-cloaker → #2 disclosure → #3 pillar IA → #4 minimalist homepage → #5 Magic Go → #6 dashboard). Moved the previous Now items into "Carried over" since they're Magic Go-orthogonal cleanup. Moved Moultrie EDGE / cellular guide / Flex G36 / satellite-rotation to "Backburner — defer until Magic Go ships" because Magic Go is how they ship.
- Captured the strategic pivot in `docs/PROJECT_STATE.md` as the 2026-05-28 milestone entry.

## Decisions

- **Magic Go is the North Star.** Everything shipped to date (voice doctrine, Bottom Line gate, 3-layer image safeguard, lint scripts + pre-commit hook, audit-product-images.ps1, /aff state machine) reframes as **foundation** for the autonomous run — they're the safety net that lets Ray trust the overnight queue. The brainstorm explicitly cuts ambient niche-scoring, domain-availability checks, 500-1k page volume play, hands-off browser-VPS automation as "not now" — Ray's gate stays Bottom Lines.
- **Item 1 (multi-network commission routing) is the highest-$ lever.** Routes through `workers/link-cloaker` extension: per-product KV with `{asin, brand, links:[{network, url, commission_rate, status}]}`, pick best-paying live link at request time. The plan says do not write Worker code until `docs/brainstorms/2026-05-29-multi-network-routing-plan.md` is written and reviewed.
- **Refuse-to-run on partial state.** Magic Go won't run for any site missing Items 1-4. Cleanliness over half-states.

## Discoveries / Gotchas

- The plan doc explicitly says "don't freelance" and "don't expand scope." Future Claude (including me on subsequent turns) needs to file new ideas through `/aff-idea` to Second Brain instead of injecting them into the active sequence.
- Five Open Items gate Item 1 implementation. The network-approval status question (which of Avant, Awin, AvantLink, CJ, Rakuten Ray is approved + can pull tracking links from right now) has no default and requires Ray's explicit answer before any code starts.

## Files Changed

- `docs/brainstorms/2026-05-28-magic-go-vision.md` — new (179 lines, copied from Ray's Desktop)
- `docs/TODO.md` — restructured around Magic Go prerequisite chain
- `docs/PROJECT_STATE.md` — added 2026-05-28 strategic-pivot milestone entry

## Status at End of Session (morning)

- All three doc changes uncommitted until the planning conversation lands. Next move: ask Ray the 5 Open Items, in particular #1 (network approval status), then commit the docs + write the multi-network routing plan.
- No code changes today. Items 1-6 stay queued.

---

## Afternoon / Evening — Magic Go pivot, supervised proof, CE review, spine built

Huge session. Ray reprioritized hard: links deferred, content + Magic Go first. Sequence of what landed:

### AvantLink unblocked + DTP tag wired
- AvantLink verification kept failing ("can't locate authentication info"). Root cause: **Cloudflare's "Automatic HTTPS Rewrites" was upgrading the snippet's `http://` src to `https://` in transit**, breaking AvantLink's exact-string match. Ray flipped that Cloudflare setting OFF, verification passed. Removed the one-time verification snippet from MWC (`ed9a6d9`); Ray can re-enable Auto-HTTPS-Rewrites now. (TODO #43 closed.)
- **DTP Amazon tag wired: `detailerpicks-20`** (`ee82642`). CE review caught DTP's `affiliate.amazonTag` was empty → every DTP link earned $0 and the tag-lint skipped silently. Swapped all 41 `tag=mywildlifecam-20` → `detailerpicks-20` across the 4 live guides + 2 prompts; tag-lint now enforces DTP (62 tags, all pass). DTP product links are direct affiliateUrl (no cloakedSlug) so the swap is the live monetization fix.

### Supervised Magic Go proof — first piece shipped
- Locked **supervised-first** autonomy (Ray: "do 2 I watch then we let it rip") — then clarified supervised IS the product; headless was the plan's stretch goal, shelved. Then: "it WILL be a command" → `/aff magic-go <N>` is the target.
- Ran the chain inline for **moultrie-edge-2-review** (`38caec9` scaffold DRAFT, `a0f240f` Bottom Line → index). Picked EDGE 2 over original EDGE (live data: original is 720p + multipack-only = phasing out; EDGE 2 is the current single-listed 36MP model). Auto-wrote the full body from the research note + Canopy; Ray approved ("looks pretty good"), flagged the who-this-is-for/who-should-skip parallelism (fixed), picked Bottom Line Option 2.
- **The proof's biggest payoff:** surfaced that the scaffolder emits PROSE-only bodies while the 5 shipped reviews use RICH FRONTMATTER (scorecard/buyIf/flaws/faq via ReviewArticle.astro). The Moultrie piece is the prose-only outlier. This is the #1 thing to fix before scaling — caught before mass-producing 25 inconsistent pages.

### CE doc-review of the Magic Go v1 plan
- Ran 3 CE reviewers (feasibility, adversarial, coherence) in parallel on `docs/brainstorms/2026-05-29-magic-go-v1-plan.md` (the plan was written by a background agent earlier). They converged on real blockers — all absorbed into the plan as a "v2 revisions" section (`939774a`): body-fill must emit rich frontmatter (V1) + multi-product guide cards (V2); wire the config adapter into the scaffolders not just the gate (V3); split lint-class vs build-class quarantine to stop a collection-poison cascade (V4); product-availability gate for dead/multipack ASINs (V5); graduated N=2→5→10→25 ladder + fresh-context-per-piece (V6); per-site allocation cap + DTP-tag prereq (V7); template voice fix hard-gated before N>2 (V8); idempotent scaffold, atomic manifest, audit once-per-run, stale-manifest auto-demote, slug-collision check (V9-V12); headless is a real build item not a drop-in (V13).

### Foundational fixes (from the review)
- **Template voice-doctrine violation fixed** (`8fa3d66`): `templates/site-template/src/pages/index.astro` shipped "honest, hands-on reviews" + "products we've researched but don't own" — every bootstrapped satellite would inherit it. Fixed the template + the live fussybean homepage + the off-doctrine fussybean tagline. Added `hands-on review` to voice-doctrine forbidden list (lint-voice couldn't catch it before — the lint gap).

### Magic Go orchestrator — build steps 1+2 (the validated spine)
- **`05be8e3`** — `scripts/lib/site-config.ps1` (`Get-SiteConfigField` two-shape adapter, resolves MWC-flat + DTP-nested) + `scripts/magic-go-readiness.ps1` (R1-R5 gate). Verified: MWC + DTP READY, FB/SA/GOG SKIP with reasons.
- **`4caa15e`** — `scripts/magic-go-allocate.ps1` (cadence-deficit + 60% per-site cap). Verified: N=25 → DTP 13 / MWC 12; N=2 → 1 each.

### fussybean
- Niche locked: **home coffee GEAR** (espresso machines, grinders, kettles, scales) + a novelty/gift sub-pillar (the giant-coffee-cup pod holder lane).
- Visual identity locked: **"looks like coffeeshops smell"** → **Variant C** of the 3 HTML mockups (`docs/playgrounds/fussybean-mockups/`). Photography-forward, dark-roast hero + golden glow, espresso/cream/caramel, Fraunces + Inter. Captured in memory `project_fussybean_visual_direction`.
- Bootstrap plan written (`docs/brainstorms/2026-05-29-bootstrap-tool-plan.md`) — hybrid script + Q&A playbook, fussybean is its first dogfood.

## Decisions
- Magic Go autonomy: supervised-first (prove body-fill), then headless later. Headless is a real build item (V13), not a drop-in.
- Ship Magic Go v1 without Aaron-doc Items 1-4; content-ready-sites-only gate.
- Default to my recommendation on judgment-call forks (memory `feedback_default_to_recommendation`).

## Discoveries / Gotchas
- Cloudflare Auto-HTTPS-Rewrites breaks http-exact-match verification snippets (AvantLink). Flip it off for verification, back on after.
- Scaffolder template ≠ shipped review format (prose body vs rich frontmatter). The single biggest Magic Go quality risk; body-fill must produce frontmatter.
- DTP had empty amazonTag = silent $0; the tag-lint skips empty tags so the safety net couldn't flag it. Now wired.

## Status at End of Session (evening)
- **Done:** AvantLink, DTP tag, Moultrie Edge 2 review LIVE, CE review + plan v2, template voice fix, Magic Go build steps 1+2 (readiness gate + adapter + allocator).
- **Next build (steps 3-8):** manifest helpers, queue render, the magic-go.md playbook WITH the rich-frontmatter body-fill (V1/V2 — the judgment-heavy part), /aff posture integration, quarantine, publish-batch. Plus restructure the Moultrie piece into the rich-frontmatter gold-standard exemplar.
- **Open loops:** DTP KV re-tag (11 cloaked entries still mywildlifecam-20 — task #55); fussybean bootstrap build (identity + niche locked, ready to execute); Ray can re-enable Cloudflare Auto-HTTPS-Rewrites.
- All work committed + pushed. Branch clean.
