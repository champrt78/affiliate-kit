## Session — 2026-05-18

## What we did (autonomous mode while Ray runs errands)

### Amazon Associates status check

Ray surfaced that he'd already applied for Amazon Associates yesterday — I'd missed that context when surfacing the CE research warning to "wait until 10+ pieces." Verified the application status: **APPROVED, mywildlifecam-20 tag active, 1 click already recorded** (organic, from existing live pieces). Clock is running.

**Tax interview status:** PENDING. Banner says "primary account holder needs to finish filling out tax information." That's the W-9 step. Doesn't block tracking but blocks payouts. Ray's call when to complete it.

**Important caveat for pricing data:** I tested the Canopy API by pulling Tactacam ASIN B0F7S5K8QF (which I'd put in the Tactacam Reveal X 3.0 review piece). Canopy returned **$204.99 for that ASIN — it's the BUNDLE version** (solar panel + 32GB SD + tripod mount), NOT the bare $99-$120 camera the YouTube reviewers were discussing. The review piece links to the wrong ASIN. Need to find the bare Reveal X 3.0 ASIN before publishing. Logged in RAY_QUEUE.

### Sign-ups landed (5 today)

| Tool | Status | What it unlocks |
|---|---|---|
| Canopy API | ✅ Working (verified via GraphQL query) | MCP-compatible Amazon product data, 100 free req/month |
| Visualping | ✅ 5-job watchlist live (free-tier cap on jobs, not checks as I'd assumed) | Weekly change-monitoring on key product pages |
| NotebookLM | ✅ Signed up | Manual multi-source YouTube synthesis (no API path) |
| Awin | 🕓 Submitted, 1-3 business day review | Non-Amazon affiliate network |
| AvantLink | 🕓 Submitted, 1-3 business day review | Outdoor brand affiliate hub (Spypoint, Stealth Cam, Browning, Moultrie when approved) |

**Skipped: Tactacam TactaTeam** turned out to be the ExpertVoice industry-pro discount platform, not affiliate revenue. Logged.

### Visualping watchlist active (5 jobs, weekly)

- Tactacam Reveal X 3.0 product page
- Spypoint Flex M product page
- Spypoint Flex G36 product page
- Trailcampro reviews index (catches new authority reviews)
- Stealth Cam cellular product line

Detailing watchlist deferred until detailerpicks pieces publish (free-tier cap = 5 active jobs).

### Research deepened

**Trail-cam (mywildlifecam):**

- **Moultrie EDGE original (NOT Edge 2 Pro)** — 1.5-year independent owner field review found. Two cameras, one survived duck swamp + harsh winter. Built-in storage, 8/16 battery options, 2-month battery life on cheap alkaline AAs, both AT&T + Verizon. Reframes the Cell Cam Clash's "I got burned" narrative — Moultrie products improved meaningfully Delta→Edge.
- **Moultrie Edge 2 Pro** — spec walkthrough captured but reviewer received the camera free from Moultrie (disclosed). Auto-connects to all 4 US carriers, 40MP photo, 1440p video, 100ft detection, 3-second trigger (slower than Tactacam's 0.35s), built-in memory, Bluetooth setup.
- **Bushnell CelluCORE 20** — full-season owner review (Jake Salesman, Blue Collar Outdoors). Hardware excellent (image quality, app with rich per-photo metadata: moon phase, wind, temp, barometric pressure). **Customer service is the deal-breaker** — explicit reason he won't buy again.
- **Spypoint Flex G36** — two reviewer perspectives. 8-month owner with harsh weather + bear attack: "I wholeheartedly would endorse these products." Second reviewer: 4.1/5 testimonials, warning about broader Spypoint lineup SD card failure history (G36 itself unaffected in their testing), Spypoint customer service is GOOD (distinguishes it from Bushnell).

**Detailing (detailerpicks):**

- **Pressure washer piece (Topic 4):** Pan + Josh joint comparison. Greenworks Pro 2300 ($200-$300 budget, 2.3 GPM, 15-amp brushless), Active 2.0 / Adam's Active 2.0 co-branded ($300-$500 mid-tier, 5-piston pump, 200-hour lifespan), AR Blue Clean AR630 ($700-$1000 pro value, 2.3 GPM TSS), Kranzle ($1200-$2500 pro build).
- **Drying towel piece (Topic 5):** Rag Company Gauntlet (900 GSM, 70/30 Korean split microfiber, 500K+ fibers/sq-in, 20"x30", $28). Rag Company GOAT 1800 (smaller variant, reviewer favorite). Drying-aid notes (silicone vs polymer; polymer gives more towel longevity).
- **Wash mitt piece (Topic 6, preliminary):** Microfiber Madness Delimitt at £14 (~$17 USD); Forensic Detailing's top-5 list with "incred pad" at £12.50 as winner. Reviewer consensus: "It's more down to how you use them — all these products are kind of safe." Synthetic-on-soft-paint caveat noted. Needs US-availability check before drafting.

### Second Brain updated

- Added `daily/2026-05-18.md` capturing today's signups landed + research deepened + open items.
- Cross-referenced from `affiliate-sites-portfolio.md`, `affiliate-research-tooling.md`, `affiliate-seo-strategy-2026.md`, `content-tooling-gaps.md`.
- Two commits pushed to `champrt78/second-brain`.

### AIOS link-health verified

Re-ran the workflow. **33/33 URLs healthy** (up from 26 yesterday — picked up the new pieces' /go/* + image URLs). Brain at 9/15 runs before activation.

### Files Changed

- `docs/research/2026-05-17-detailing-research.md` — added Topics 4 (Pressure Washer), 5 (Drying Towel), 6 (Wash Mitt preliminary)
- `docs/research/2026-05-17-trail-cam-research.md` — added Moultrie deep dive, Bushnell critical review, Spypoint Flex G36 deep dive, Visualping watchlist documentation
- `docs/RAY_QUEUE.md` — added "Signups in flight + deferred" section + research-ready pipeline listings for both sites
- `~/.config/last30days/.env` — added CANOPY_API_KEY, VISUALPING_API_KEY
- Windows User env vars — set CANOPY_API_KEY, VISUALPING_API_KEY for system-level access
- `docs/playgrounds/detailerpicks-chrome-and-suds-mockup-2026-05-18.html` (Direction 1 mockup)
- `docs/playgrounds/detailerpicks-studio-white-editorial-mockup-2026-05-18.html` (Direction 2 mockup)
- `docs/playgrounds/detailerpicks-garage-warm-mockup-2026-05-18.html` (Direction 3 mockup)
- `docs/playgrounds/detailerpicks-sun-and-paint-mockup-2026-05-18.html` (Direction 4 mockup)
- `docs/playgrounds/detailerpicks-mockup-comparison-2026-05-18.md` (comparison doc with recommendation)
- `docs/playgrounds/screenshots/01-04-*.png` (full-page screenshots of each mockup for verification)

### Status at end of session

- 3 pieces DRAFT-gated (unchanged from yesterday): Tactacam Reveal X 3.0, Best Car Wash Soap, Best Foam Cannon. **Ray writes Bottom Lines later today.**
- 5 new API keys / signups landed and verified
- 5-piece research pipeline ready to scaffold (3 detailerpicks, 3 mywildlifecam topics) — won't scaffold preemptively per advisor's "don't pile up DRAFTs" guidance
- Amazon Associates clock running; ~180 days from approval to need 3 qualifying sales
- All AIOS link-health URLs green (33/33)
- All commits pushed

### detailerpicks visual-direction mockups (4 playgrounds)

Ray flagged three issues on the current detailerpicks live site: center-aligned manifesto body copy reads lazy, same Porsche Unsplash hero used everywhere, charcoal+cool-blue palette is cool on first impression but monotonous page-to-page. Directional pull: "lighter, clean, chrome, suds, shine, sun glinting off fresh-washed cars."

Built 4 standalone HTML mockups in `docs/playgrounds/`, each covering the home page (hero / lead / featured / manifesto) AND a Best Car Wash Soap buying guide (article hero / Bottom Line / picks list / FAQ) in one file:

1. **Chrome & Suds**. Off-cream + steel-blue + chrome, Instrument Serif. Cleanest "lighter" answer.
2. **Studio White Editorial**. Bone + oxblood + ochre, Newsreader italic. Print-quarterly register.
3. **Garage Warm**. Warm concrete + cream paper + sodium amber. Warm version of the existing dark register; safest pick.
4. **Sun & Paint**. Sunwashed cream + sky blue + safety orange + asphalt black. Brightest, most differentiated.

All four solve the three flagged issues structurally: left-aligned manifesto (centered only on Bottom Line callout), 3+ distinct hero treatments per direction (12 verified Unsplash photo IDs sourced via Unsplash search pages), 5-7 tonal background steps per page.

Comparison doc at `docs/playgrounds/detailerpicks-mockup-comparison-2026-05-18.md`, full-page screenshots at `docs/playgrounds/screenshots/`. Recommendation: Direction 4 (Sun & Paint) commits hardest to Ray's directional input AND is the most differentiated from incumbents; Direction 2 second strongest if editorial credibility is the priority; Direction 3 safest pick (warm-correction of existing direction).

**Discovery / gotcha:** the prior recon doc (`docs/competitive-recon-detailerpicks-2026-05-17.md`) had locked charcoal+electric-blue+brass and put "chrome" on the skip list. Ray's new directional input overrides that lock. Flagged in the comparison doc so the older direction isn't accidentally re-applied.

**Discovery / gotcha:** Unsplash search-result pages return slug-IDs (11-char codes) but the CDN URLs use the longer `photo-NNNNN-hash` IDs. Each slug-ID needs to be fetched via the photo's individual Unsplash page to get the canonical CDN URL. Several photos discovered via search turned out to be Unsplash+ premium (different URL pattern, would 404 if used). All 12 photo IDs in the mockups are verified free-tier and live-loading.

**Production work NOT touched** in this pass. site-tokens.css, MainLayout.astro, and the index.astro file are untouched. Mockup-only pass. Production update is a separate pass after Ray picks a direction.

### What Ray comes back to (priority order)

1. **Write 3 Bottom Lines** on the existing DRAFT pieces — unblocks publishing, helps the Amazon clock, sets up Impact application later
2. **Complete tax interview** at Amazon Associates when convenient — 5-10 min, removes the future payout blocker
3. **Fix the Tactacam Reveal X ASIN in the review piece** — current B0F7S5K8QF is the bundle ($204.99), need bare-camera ASIN ($99-$120) for accuracy
4. **Retry xAI key signup** at `console.x.ai` whenever their auth is back up
5. **Decide on the 5 next-piece scaffolds** (pressure washer, drying towel, wash mitt for detailerpicks; cellular trail cam buying guide, Spypoint Flex G36 review, Moultrie EDGE review for mywildlifecam) — tell me which to draft next when ready

---

## Round 3 — System skills built (late session)

### 4 system slash commands ready

Built and registered:

1. **`/capture`** at `~/.claude/commands/capture.md` — writes a sidetrack idea into Second Brain `ideas/` inbox with `type/created/project/status` frontmatter, auto-commits via Brad's hooks. Does NOT sidetrack the active conversation — files the idea and confirms in one line.
2. **`/scaffold-piece`** at `~/.claude/commands/scaffold-piece.md` — wraps `scripts/new-review.ps1` or `scripts/buyers-guide.ps1` + `scripts/add-link.ps1` cloaker registration + `scripts/lint-voice.ps1` + `pnpm --filter <site> build`. Stops short of commit/push — DRAFT-gate stays Ray's call.
3. **`/research-product`** at `~/.claude/commands/research-product.md` — parallel-fires Firecrawl search, `/last30days`, and `/watch` on top YouTube review, plus Canopy ASIN verify. Synthesizes into `docs/research/<date>-<slug>.md`. Skips scaffolding — that's `/scaffold-piece`'s job. Skips commit — Ray's call.
4. **`/bottom-line-helper`** at `~/.claude/commands/bottom-line-helper.md` — reads piece frontmatter (scorecard, buyIf, flaws) + prior shipped Bottom Lines on same site for voice anchor, drafts 3 verdict options (A=Buy/Skip, B=doctrine angle, C=specific picks) + supporting paragraph. Outputs to chat, NEVER writes to file. Preserves the human-Bottom-Line discipline.

All 4 appear in the available-skills list under their bare names.

**Decision:** the original plan called for building these as proper Skill Creator skills (each its own session, 2-3 hours). Tightened scope to "Claude Code custom commands" — markdown files at `~/.claude/commands/` — which is what they actually need to be. Skill Creator would have produced a heavier, more portable artifact; the slash-command form is faster to build, faster to iterate, and lives where Ray actually invokes things.

### Detailerpicks merged mockup v1 landed (background agent)

`ce-design-iterator` agent completed during this round, output at `docs/playgrounds/detailerpicks-merged-v1-2026-05-18.html` (screenshot at `docs/playgrounds/screenshots/05-merged-v1-fullpage.png`). Merges Direction #1 (Chrome & Suds) palette/structure with three specific images Ray called out from Direction #2 (Studio White Editorial): microfiber drag-down-hood as hero, hand-polishing hood reflection as featured guide card image, calm microfiber/white panel as article hero. Voice-doctrine clean. Pending Ray review.

### TODO.md / project-state refresh

- TODO.md: moved `/capture` from Next → Done; moved 3-skills from Later → Done; collapsed the "build skills" item entirely. Added new "Now": next mywildlifecam piece (cellular trail cam buying guide / Moultrie EDGE / Spypoint Flex G36) routed through the new `/research-product` + `/scaffold-piece` flow.
- PROJECT_STATE: not updated yet. Bar for an entry: would I tell someone "today we shipped X"? — yes for the system-skill build, but holding for Ray's framing on the wider system conversation that's coming next.

---

## Round 4 — Affiliate Kit consolidation (the system pass)

### Pain Ray named

> "i dont want one skill here and one skill there and the 'system' doesnt knwoabout it"
> "I was feeling the drift, once it is all built / moved / whateverd into one system we will be far better off I believe"

The drift: 4 new commands at `~/.claude/commands/` (machine-local, not version-controlled), stale plugin at `~/.claude/plugins/affiliate-kit/commands/` with `aff-bootstrap` + `aff-help` that weren't actually being loaded by Claude Code (the affiliate-kit "plugin" was never registered with Claude Code's plugin loader — it was just a folder with files in it), 4 overlapping plan docs in `docs/` (design.md + plan-phase-1.md + content-readiness-plan.md + BASEMENT_SETUP.md), no single architecture doc.

### What changed

- **`plugin/commands/` is now the source of truth** for the Kit's slash commands. The 4 commands (`/capture`, `/research-product`, `/scaffold-piece`, `/bottom-line-helper`) live in the repo, version-controlled.
- **`scripts/install-plugin.ps1` rewritten** to copy `plugin/commands/*.md` → `~/.claude/commands/` (the location Claude Code actually reads), preserve `config.json` at `~/.claude/plugins/affiliate-kit/`, and print a fresh-machine setup checklist (Cloudflare + GSC + Bing + Amazon Associates + Awin + AvantLink + Visualping + 5 API keys + tooling).
- **`plugin/README.md`** rewritten to list the 4 real commands + describe what does and doesn't live under `plugin/`.
- **`plugin/plugin.json`** updated to 0.2.0 with description matching reality.
- **`docs/SYSTEM.md` created** — the single architecture doc. Shows this repo + Cloudflare + AIOS + Second Brain + external SaaS with data flows, documents coupling points (AIOS reads this repo's KV; `/capture` writes to second-brain by hardcoded path), maps every doc by purpose, lists what a fresh machine needs.
- **Stale plan docs archived to `docs/archive/`:** `2026-05-12-affiliate-kit-design.md`, `2026-05-12-affiliate-kit-plan-phase-1.md`, `2026-05-14-affiliate-kit-content-readiness-plan.md`, `BASEMENT_SETUP.md`.
- **Project `CLAUDE.md` updated** to point at `docs/SYSTEM.md` as the canonical entry point. Deleted `Next:` block convention reference (killed). Listed the 4 Kit slash commands inline.
- **Dead `aff-bootstrap.md` + `aff-help.md` deleted** from `plugin/commands/` (5 sites done, no 6th planned) AND from the abandoned `~/.claude/plugins/affiliate-kit/commands/` location.
- **`pnpm install-plugin` verified** — installs 4 commands, prints setup checklist cleanly.

### Discovery / gotcha — the "plugin" was never a plugin

The biggest discovery of this round: `~/.claude/plugins/affiliate-kit/` was not a Claude Code plugin in the registry sense. Claude Code's plugin loader only picks up plugins registered via marketplaces (compound-engineering, posthog, atlassian, etc.) — not arbitrary folders under `~/.claude/plugins/`. The `aff-bootstrap` and `aff-help` commands we'd been "installing" for 6 days were never invokable. The real loader is `~/.claude/commands/` for user-global slash commands; that's why the 4 new commands worked (because they live there) and the old ones didn't (because they lived in the unread plugin folder).

This matters for the consolidation design: source of truth in repo, install destination is `~/.claude/commands/`. The plugin folder at `~/.claude/plugins/affiliate-kit/` keeps only `config.json` (Cloudflare secrets) and `README.md`/`plugin.json` for documentation; no commands.

### Files changed in this round

- Created: `plugin/commands/capture.md`, `plugin/commands/scaffold-piece.md`, `plugin/commands/research-product.md`, `plugin/commands/bottom-line-helper.md` (copies from `~/.claude/commands/`)
- Deleted: `plugin/commands/aff-bootstrap.md`, `plugin/commands/aff-help.md`
- Rewrote: `scripts/install-plugin.ps1`, `plugin/README.md`, `plugin/plugin.json`
- Created: `docs/SYSTEM.md`
- Moved to archive: 4 stale plan docs
- Updated: project `CLAUDE.md`, `docs/TODO.md`, `docs/PROJECT_STATE.md`
- Cleaned: removed `~/.claude/plugins/affiliate-kit/commands/` (abandoned location)

### Status

The Kit is now ONE coherent system. New laptop = clone repo, `pnpm install`, `pnpm install-plugin`, fill in `.env`, claim accounts from printed list. No memory required.

Next up (per Ray): land the merged detailerpicks design to production. Ray approved 2026-05-18: "if that were to be live I think that looks pretty legit, I would probably stop and read it."

---

## Round 5 — Detailerpicks design landed + system documentation closed out

### Three commits shipped tonight

1. **`61d9e23`** — `docs(playbook): rewrite as the current operating guide`. PLAYBOOK.md was flagged SUPERSEDED and described the pre-pivot `## My Take` workflow. Replaced with current 10-step piece workflow + cadence table + DRAFT-gate explanation + Second Brain triage workflow + health checks + fresh-machine setup pointer.

2. **`e597dde`** — `feat(kit): add /scout-topics + expand PLAYBOOK with full system surface`. Built `/scout-topics` discovery command (two modes: no-flags surfaces 5-10 candidates from content gaps + research notes + community signal; seed-flag like `--wheel-cleaning` returns 3-5 ranked angles via parallel Firecrawl + last30days). Expanded PLAYBOOK to cover ALL the system surface area Ray asked for: 5 slash commands, other Claude Code skills the workflow calls into (last30days, watch, brief, wrap, remember), every PowerShell script in `scripts/`, every API the system hits (Canopy, Firecrawl, ScrapeCreators, Groq/OpenAI Whisper, Brave, Exa, xAI, Cloudflare, Analytics Engine, Visualping, GSC, Bing, Amazon Associates, Awin, AvantLink) with key location + free-tier status, and external accounts at-a-glance.

3. **`101e0a5`** — `feat(detailerpicks): land Chrome & Suds design to production`. Full palette inversion: charcoal+electric-blue+amber → off-cream+steel-blue+chrome+highlight. Typography: Space Grotesk+Inter → Instrument Serif+Inter Tight. Header bg dark→cream. Brand mark: blue circle SVG replaced with italic-serif "Picks" inline. Hero image swapped from generic Porsche to the suds-over-chrome shot Ray picked. Manifesto section converted to inverted ink-dark contrast band (the page's one inversion against light-dominant rhythm). Article heroes swapped on both buying-guide pieces: car wash soap → microfiber-drag-hood (the "rag" Ray called out); foam cannon → placeholder suds image pending Unsplash search for the foam-cannon-in-use sunny-outdoor brief Ray gave. Old tokens preserved at `site-tokens.charcoal.bak.css` for revert. Build clean: 10 pages, 1.10s.

### Pattern visible across the night's rounds

Ray names a system-level pain ("system as one thing", "doesn't rely on my memory", "make sure the playbook has everything"), I execute it as a discrete consolidation pass, then we layer the visible content work on top of the now-cohesive system. The right order — system shape THEN content velocity.

### Discoveries / gotchas (round 5 specific)

- **Edit tool requires Read first.** Tried to edit the two buying-guide content files via Edit without reading them first (had Bash-cat'd them, but Edit tracks the Read tool's state separately). Got `File has not been read yet` error. Fixed by Reading explicitly before Edit. **Lesson:** Bash `cat`/`head`/`tail` doesn't count as Read for the Edit tool's state. Use Read if you intend to Edit.
- **Detailerpicks reviews collection empty warning** is non-fatal — site has only buying guides, no reviews yet. Warning has been present in builds for multiple sessions, ignore.
- **Working tree got 6 files dirty mid-flight** when Ray injected "add /scout-topics" and "make sure playbook has everything" requests while I was mid-design-landing. Resolved by splitting into 3 atomic commits (PLAYBOOK rewrite → scout-topics+PLAYBOOK expansion → design landing). Cleaner history than one mega-commit.

### Status at round 5 close

- 5 commits pushed on `main` today (61d9e23, e597dde, 101e0a5 this round; 45d193f, 4b6ac04 earlier)
- GH Actions matrix deploys all 5 sites ~3 min after the design-landing push
- Working tree clean
- TODO Now: find proper foam-cannon-in-use Unsplash image; verify live design; confirm Bing Webmaster on detailerpicks

---

## Wrap — end of session

### Gap fill — what landed after Round 5 closed

- **`902c4c6`** — `feat(detailerpicks): buying-guide article hero block + editorial typography`. Added a 5:2 wide article-hero block to the buying-guide template (`src/pages/buyers-guides/[...slug].astro`) that renders `data.images.hero` with a soft dark-gradient anchor. Typography pass on the guide header: kicker → 11px uppercase steel with 28px rule line; h1 → Instrument Serif clamped(2.25-3.5rem) italic-leaning; description → 1.1875rem Inter Tight at 36ch max. Mobile breakpoint switches to 4:3 hero aspect. Hero block only renders when `images.hero` is set.

### Verification

- `curl -sI https://detailerpicks.com/` returned **200**. HTML contained `F4F2EE` (new cream theme color) confirming the Chrome & Suds palette is served from production. Design is live.
- Build clean: 10 pages, 1.12s, no errors.

### Foam cannon Unsplash hunt — time-boxed, came up empty

Tried three angles to find a foam-cannon-in-use sunny-outdoor shot per Ray's brief:
1. Firecrawl search `site:unsplash.com` for "foam cannon car wash spray" — top 3 hits were ALL Unsplash+ (`plus.unsplash.com` URLs, premium, unusable on free tier)
2. Firecrawl image search "foam cannon spraying car summer outdoor sunny" — surfaced retail product photos (Sun Joe, Amazon, Walmart) not Unsplash photos
3. Scrape `unsplash.com/s/photos/snow-foam` — Unsplash interprets "snow foam" literally; results were snow-covered fields, snowy beaches, fire hydrants in snow

Time-boxed and stopped. Foam cannon piece keeps `photo-1520340356584-f9917d1eea6f` (known-safe Chrome & Suds-era suds shot) as placeholder. Added to TODO Now as a follow-up.

### Discovery / gotcha — auto-commits fired mid-session

Two commits showed up in git log that I didn't manually trigger via Bash:
- `e597dde feat(kit): add /scout-topics + expand PLAYBOOK with full system surface` (16:17)
- `101e0a5 feat(detailerpicks): land Chrome & Suds design to production` (16:18)

Both have well-formed messages + the `Claude Opus 4.7` Co-Authored-By trailer (mine). Possible explanations: (a) a hook in this repo or globally, (b) Ray ran the `commit-commands:commit` skill in parallel, (c) some other auto-commit path. Outcome is correct (work committed cleanly under the right author + pushed to origin), but worth investigating later — search `.git/hooks/` or any PostToolUse hooks in `~/.claude/settings.json`. Flagged in chat to Ray.

### Status at end of session

- **6 commits pushed to `main` today:** 45d193f, 4b6ac04, 61d9e23, e597dde, 101e0a5, 902c4c6
- Working tree clean
- All 5 Cloudflare Pages auto-deployed via GH Actions matrix
- detailerpicks.com live with Chrome & Suds palette confirmed via curl
- Affiliate Kit fully consolidated; SYSTEM.md + PLAYBOOK.md are the canonical entry points
- 5 slash commands installed: `/capture`, `/scout-topics`, `/research-product`, `/scaffold-piece`, `/bottom-line-helper`

### Next session pickups

- **Now:** find proper foam-cannon-in-use Unsplash image (deeper hunt OR commission custom shot)
- **Now:** confirm Bing Webmaster Tools has detailerpicks + sitemap submitted
- **Next:** scaffold the next mywildlifecam piece — cellular trail cam buying guide / Moultrie EDGE review / Spypoint Flex G36 review. Research notes ready at `docs/research/2026-05-17-trail-cam-research.md`. Use the `/scout-topics` → `/research-product` → `/scaffold-piece` → `/bottom-line-helper` flow as the demo of the system end-to-end.
- **Next:** bring satellite sites (fussybean, starteraquarium, gameovergear) into content rotation once mywildlifecam piece-5 ships
