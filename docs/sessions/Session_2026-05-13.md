# Session — 2026-05-13

> Most of today's work was on the sibling `carillon-station` repo. See that repo's `docs/sessions/Session_2026-05-13.md` for the full session log. This file covers only the affiliate-sites-specific work.

## What We Did

- **Wrote a single-file HTML launch playbook** at `docs/launch-playbook.html` covering brand-launch tasks for BOTH brands (Carillon Station + Semper Fi Studios). Single source of truth Ray follows tonight during basketball. 12 numbered step cards with checkboxes that persist via localStorage. Mobile-responsive, print-friendly, no external deps.
- **Three iterative refinements** as Ray pushed feedback:
  1. Initial 12-step playbook (commit `d0bbe61`)
  2. Per-platform fallback framework added after Ray pushed back on rigid unified-fallback thinking — decision-framework section + per-platform priority tables + Step 2 rewrite + defensive-accounts definition (`a723b76`)
  3. Barney-style rewrite when Ray asked for hand-holding ("click this, then this") — Tonight at a glance TL;DR + email callouts + Step 4/5/6 click-by-click Cloudflare + Porkbun walkthroughs + Step 7 copy-paste bio table (`013ee13`)
- **Authorship rewrite via git filter-branch.** Discovered every prior commit on `affiliate-sites` was authored by Ray's Arrow work email instead of his personal `champrt78@users.noreply.github.com`. Rewrote 13 commits across `main`, cleaned `refs/original/` backups, force-pushed. Single-author commit graph across all refs now.

## Decisions

- Launch playbook lives in `affiliate-sites/docs/` rather than in either brand's own repo because it covers BOTH brands and the Carillon Station repo is busy with the Phase 1-11 implementation work.
- Per-platform fallback strategy (not unified) for Semper Fi Studios — mixed handles across platforms are fine for a service business; the dominant-platform-only-pivots rule keeps things sane.
- Force-push for the authorship rewrite is acceptable: solo private repo, no other contributors, deployed artifacts on Cloudflare Pages aren't affected by SHA changes (they redeploy at build time, not at commit-SHA time).

## Discoveries / Gotchas

- `--force-with-lease` fails after `git filter-branch` because remote-tracking refs got rewritten too — treated as stale info. Plain `--force` is required.
- A leaked `gh` token (champrt78) ended up in stdout during a `git push` earlier (URL-embedded token form). Captured in the launch playbook as Step 9 — `gh auth refresh -u champrt78`. Same exposure note exists in the carillon-station session log.

## Files Changed

- `docs/launch-playbook.html` — NEW; single-file HTML brand launch playbook (~1000+ lines including embedded CSS + JS)

## Late-session addition — Handle reclamation research + click-to-signup UX

After Ray flagged that (a) he remembered me saying handles could be acquired from people in a prior session, and (b) the playbook needed direct-action links ("i click on the thing it goes to tiktok create new accont"), did a web-research pass on tier-1 platform handle-reclamation policies and reworked the playbook UX.

- Spawned `ce-web-researcher` for current (2025–2026) handle-acquisition policies across IG, TikTok, X, YouTube, Facebook, LinkedIn, Threads, Bluesky. Findings: most platform "inactive username" policies are dead letters; the path that actually works is DM-the-holder. X is the only platform with an official marketplace (rented, not owned). Bluesky has the DNS escape hatch.
- Added prominent **green signup buttons** (`.signup-btn`) and **orange reclamation buttons** (`.reclaim-btn`) — click → drops Ray directly on each signup or trademark/impersonation form. No more hunting through text cells.
- Inserted **Step 2b — Handle taken? Here's what actually works per platform** between Step 2 and Step 3. Includes a 7-row reclamation cheat sheet table (verdict + trademark form + DM strategy + gotchas per platform), the DM playbook (5 steps), and a Semper Fi-specific section addressing the YouTube `@SemperFiStudios` (held by andrew docherty) situation.
- Updated TOC, references section (added "Direct signup links — Tier 1" subsection + "Handle reclamation forms" subsection with 9 direct links), and made the YouTube TAKEN row in the availability snapshot a clickable anchor to Step 2b.

## Honest moment on memory

Ray said "you told me that in a previous session" re: handle reclamation. Checked all sessions logs (this repo + carillon-station sibling repo) + memory directory — could not find any prior record. Was honest with him about it rather than playing along. The substance was correct regardless and got documented for real this time.

## Status at End of Session

- Working tree clean. Branch `main` synced with origin.
- Launch playbook is browser-bookmarkable both locally and on GitHub.
- Single-author commit graph across all refs (`champrt78 <champrt78@users.noreply.github.com>`).

## Next Steps (for Ray)

- Use the launch playbook tonight during basketball. ~2 hours of work covering token rotation, handle eyeballing, USPTO TESS scan, domain purchase, social signups, Gmail 2FA. Steps tagged "this week (NOT tonight)" handle the Cloudflare Email Routing + defensive accounts + branded-email swaps later.

---

## Session End Safety Net — 2026-05-13 16:09:21

_Auto-appended by SessionEnd hook. Captures git activity only — NOT decisions, gotchas, or reasoning. Run `/wrap` during the session for the full record._

### Commits today

```
fa4bfa0 docs(session): add 2026-05-13 session log + PROJECT_STATE entry for launch playbook
013ee13 docs(playbook): Barney-style rewrite for tired-Ray ergonomics
a723b76 docs(playbook): per-platform fallback logic + defensive accounts definition
d0bbe61 docs: add brand launch playbook for Carillon Station + Semper Fi Studios
```

