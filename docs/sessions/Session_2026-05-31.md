# Session — 2026-05-31

(Continuation of the 2026-05-30 late session; date rolled over mid-work.)

## What We Did
- Ray said "fix everything" on the front-end audit report. Applied all remaining actionable findings + the structural follow-up.
- **Sparse grids (audit should-fix #2)** (`cce8240`): 13 listing/home/topic grids used hardcoded `repeat(2/3, minmax(0,1fr))`, so few items stretched or left orphan rows (MWC guides 2+1, gog guides 6+1, DTP home single review alone). Switched to `repeat(auto-fit, minmax(240px,300px))` + `justify-content:center` (matches locked template). Excluded the `[...slug].astro` in-guide 2×3 product grids on purpose. Allowlist python script (`.tmp/fixgrids.py`), not a blind sweep.
- **fussybean hero contrast (nit #3)** (`cce8240`): brass `.hero h1 em` accent read low over the latte photo (parent h1 shadow blur=30 too diffuse). Added a tight dark halo `text-shadow: 0 1px 8px rgba(0,0,0,.6), 0 0 2px rgba(0,0,0,.5)`.
- **Structural header fix** (`a3b44f0`): removed the `.page-header__inner` grid landmine across 13 files + added `scripts/lint-page-header.ps1` to the pre-commit hook. See Decisions + Gotchas.
- **Library 04 — Bottom Line polish** (`docs/playgrounds/library/04-bottomline.html`): Ray said "close but not close enough, needs something." Diagnosed it as the right rail floating as 3 disconnected elements in dead space. Fix = wrapped image+price+button into a self-contained **buy card** (`.bl__card`). One move solved column separation + the floating feel + spacing rhythm at once. Secondary polish: ✓/✕ became circular badges (filled accent for buy, outlined for skip), price grew to serif 26px (conversion element), `align-items: start`, rail track 210→248px, accent-arrow micro-interaction on hover, eyebrow got a brass tick mark. Advisor agreed buy-card was the single "something" and warned not to add a divider on top of it (box-in-box clutter) — held that line.
- **Library 12 — Section header REDONE v2 (3x-rejected)** (`docs/playgrounds/library/12-section-header.html`): Ray rejected the prior version three times — "still left justified on the left half of the page, the big bold hero title should not wrap at all, feel like it needs to stretch the entire component." Diagnosis: title+lede capped to a narrow LEFT measure (`max-width:21ch`/`58ch`) inside an 880px column while the byline spanned full width → lopsided "left half" read. Two-part fix (advisor confirmed both required): **(1)** widened `.gh__inner` 880→1104px, dropped the title width cap, bumped title to `clamp(32px,3.9vw,45px)` + `white-space:nowrap` so the big serif title fills the component on ONE line; **(2)** added a two-column band under the title = lede left + a "What's inside" quick-facts panel right (picks · price range · best for) so the horizontal space is used and no skin reads left-heavy (the Wirecutter/RTINGS move). Kicker became an accent pill balancing the breadcrumb across a new topline row. Byline footer spans full width.

- **Rolled the GuideArticle pilot to the other 4 sites** (`b1e38f0`, branch `rebuild/component-library`): replaced each bespoke ~800-line `buyers-guides/[...slug].astro` (mywildlifecam, fussybean, gameovergear, starteraquarium) with the thin `<GuideArticle>` wrapper, mirroring DTP. 4 files, +48/−3153. Per-site text passed as props: `siteName`, `kicker={data.rubric ?? "<fallback>"}` (reads per-guide rubric, not a hardcoded site string), `editorialTeamName`, `bestForFact` (site niche). isDraft→robotsContent, bgTheme, productSchema JSON-LD preserved in each wrapper. Skin comes free from each site's site.css tokens.
  - **Content shape was uniform** — all 4 cold sites' guides carry the same frontmatter as DTP (`rubric`, `deck`, `products[]` with `name/brand/affiliateUrl/image/bestFor/priceFrom/priceUnit/hook/reason/facts/body`, `bottomLine.verdict`, optional `faq`). No hard-required field was missing; all 4 built green first try. No content invented.
  - **All 4 build GREEN.** Screenshot-verified one `best-X` guide per site at 1440: header + "What's inside" facts panel, Bottom Line buy-card (price + product image), "The Once-Over" 3-card grid with award banners, "The full detail" deep cards with contain-on-white images, editorial spec table, FAQ, footer — all render. Skins correct: mwc forest/cream, fussybean espresso, gameovergear dark+neon-cyan, starteraquarium aqua+coral.
  - Verified the noindex gate (`<meta name="robots" content="index, follow">` on a complete guide), per-guide kicker (`Arcade Sticks · Buying Guide` on GOG arcade-sticks, NOT the site fallback), and per-site byline ("The GameOverGear desk").

## Decisions
- **GuideArticle roll-out: mirror DTP structurally, props only for per-site text.** Did NOT re-add the per-wrapper `faqLd` FAQPage JSON-LD or `ogImage` that the old bespoke pages emitted — re-adding them per-site would re-create the exact divergence this rebuild deletes. DTP's pilot wrapper omits both too. Their correct home is the shared `FaqList` / a shared head — out of scope this task. Filed as follow-ups (see Gotchas). Confirmed with advisor.
- **kicker reads `data.rubric`, not a hardcoded site string.** Caught before writing: the bespoke pages rendered each guide's own `rubric` frontmatter (FB/GOG/SA span multiple categories per site), so hardcoding one site string would flatten distinct per-guide rubrics. Wrapper passes `kicker={data.rubric ?? "<site fallback>"}`.
- **Structural header "never again" = lint, not component.** Ray green-lit extracting a shared `PageHeader.astro`. On inspection the title typography is **3-way per-skin divergent** (gog: line-height 1.45 / no display tracking; fb/sa: clamp + tracking baseline; mwc: adds `opsz 60` + `capitalize`). A clean component would flatten those or require porting 3 skin variants into site.css + verifying ~12 page/skin combos — a daylight task, high regression risk at 8am on the exact CSS Ray's been frustrated by. Two advisor calls agreed: do **cleanup + lint** instead. The lint blocks the *exact* failure signature (`display:grid`/`grid-template-columns` on `.page-header__inner`) at commit time — a stronger guarantee than a component for this specific bug. Component extraction filed as deferred task #94 (advisor: dedup the identical *layout* CSS into a shared stylesheet, lower-risk than a markup component).
- Grid cap = `minmax(240px,300px)` to match the locked site-template, not the audit's literal `280-360` — consistency over bikeshedding.

## Discoveries / Gotchas
- **Root cause of the recurring header split**: `.page-header__inner` carried a stale `display:grid` — the listing pages had the *original* 2-col `minmax(0,1fr) minmax(0,1fr)`; the guide-indexes had `auto-fit` grid **bled in from my earlier card-grid find-replace sweep**. Both were masked by an appended `display:block` override patch. So every header was "a grid rule fighting an override patch" — drift-prone, and the source of the "header is only half the page" complaint. Cleanup removes the grid entirely; render is unchanged because the override already forced block (verified).
- `--color-forest` is NOT bleed — it's a deliberate **shared semantic token** (heading-ink slot); each skin maps its own ink onto it (espresso #241710, teal #0c5470, gog light-ink #ede9ff). mwc references it but defines it in a base/token file, not site.css (out of scope, not flagged by audit).
- agent-browser device emulation is **macOS-only** — can't set a mobile viewport on Windows. Used the Playwright MCP (its own headless Chromium) for the 390px mobile check instead.
- auto-fit + `justify-content:center` on a lone card computes to tracks `300px 0px 0px` — centered per computed style, but collapsed-track gaps lean it slightly left. Negligible; the real bug ("card too big") is fixed by the 300px cap.
- **Bottom-line buy card / gog fill direction**: card uses `--paper` fill + `1px --border` + soft shadow so it reads "raised" regardless of fill direction. On the 4 light skins `--paper` is lighter than the `--paper-muted` panel (raised — good). On **gog**, `--paper` (#0e0b1c) is *darker* than the panel (#171331), so the card read recessed. Fixed with a per-skin `--card-bg` token (gog overrides to #1f1a45, lighter than its panel) — `.bl__card` falls back to `--paper` for the other 4. sa (white card on white panel) is carried by the border+shadow alone. Both verified by screenshot — they were the pass/fail gates.
- **Library 12 nowrap measurement trap**: a `white-space:nowrap` block-level title reports `scrollWidth === containerWidth` (it stretches to fill), so my first overflow check showed a false `slack: 0` on all serif skins and a false "overflow" on FB. Real text width needs `Range.getBoundingClientRect()` on the node contents, not `scrollWidth`. True measurements: FB (longest serif title, "...Espresso Machines Under $1,000") = 993px text in a 1088px inner → 95px slack; mwc 258, dtp 446, sa 407, gog 822. All fit with margin at 45px. Landed the clamp ceiling at 45px (not the 52px I first tried, which genuinely overflowed FB by ~46px) for a safety cushion against per-serif font-load width variance across the 5 skins. **gog is the sanctioned nowrap exception** — Press Start 2P would overflow catastrophically, so it keeps the per-skin override (19px, `white-space:normal`, wraps cleanly to 2 lines). Facts panel uses only pre-existing vars (`--paper-muted`, `--border`, `--muted`, `--ink`, `--brass-soft`, `--accent`) — zero changes to the locked 5 skin blocks or page scaffold (git-diff verified). Price ranges use `&ndash;` (en dash, correct), no em dashes, no hands-on-use claims.

## Verification (Ray's open wound = claim-without-verify — so everything screenshot-checked)
- Lint tested **both directions**: cleaned state PASSES (13 blocks), planted `display:grid` FAILS, `@media grid-template-columns` FAILS. Live pre-commit hook ran it on the actual commit — passed.
- Full `git diff` of all 13 cleaned files read end-to-end (not spot-checked) — uniform deletion set, `opsz 30` on how-we-evaluate correctly preserved.
- Screenshots: gog buyers-guides index (header full-width stack + capped centered grid), gog controllers topic hub (header stack + capped card), DTP how-we-evaluate (structural-variant nested markup, header correct), gog buyers-guides @390px mobile (no horizontal overflow, header block, single-col grid).
- All 5 sites build green after each change batch.

## Files Changed
- `scripts/lint-page-header.ps1` — NEW. Block-aware lint, fails on grid in `.page-header__inner`.
- `scripts/pre-commit-hook.sh` — wired the new lint (triggers on staged `sites/*.astro`).
- 13× page files — grid landmine removed from `.page-header__inner` + override patches deleted.
- 13× listing/home/topic grids — `auto-fit` + center (overlaps the page files; fussybean index also got the hero contrast fix).

## Status at End of Session
- All "fix everything" audit items DONE + pushed (`cce8240`, `a3b44f0`); 5 sites auto-deploying.
- Header-split regression now **actively blocked** by pre-commit lint — can't recur silently.
- Open: task #94 (deferred PageHeader component, not urgent — lint covers the regression), task #93 (surface 3 stranded DRAFTs to dashboard — needs Ray's verdict picks, offered).

---

## Library mockup polish — 04-bottomline + 05-pickcard-glance (5-skin standalones)

### What We Did
- Polished both "close but not perfect" library component mockups Ray pushed back on. Two surgical CSS changes total (file 1 got the balance fix; both files got a latent image-containment fix). No structural/overhaul changes — proportions held sacred.
- **04-bottomline — left/right vertical balance (the actual "left feels sparse" complaint).** Changed `.bl__inner` `align-items: start` → `stretch` and made `.bl__main` a flex column with `justify-content: space-between`. The left verdict column now distributes to the buy-card's height: eyebrow pins to the card top, "Heads up" caveat pins to the card bottom (level with the buy button). Measured: card was 67–99px taller than main on every skin (worst on gog/dtp/fb — shortest copy); after, main==card height exactly (diff 0) on all 5. The Buy-row divider + caveat hairline make the redistributed space fall into structured gaps, not one void.
- **Both files — latent image-containment bug.** Product `<img>` had `max-height` but no `min-height:0`, so the flex/grid item's `min-height:auto` floor let a tall real PNG escape the box. Added `min-width:0; min-height:0` to `.bl__img img` (file 1) and `.qcard__img img` (file 2).

### Decisions
- Chose Ray's "distribute" framing (space-between) over "center" (`align-items:center`). Center would unmoor the eyebrow from the card's top edge; distribute keeps eyebrow→top, caveat→bottom — a deliberate two-column block. Verified on gog (biggest gap) and mwc (smallest) — both read finished, neither stretched.
- Left badges, divider, caveat untouched — already read as intentional (filled ✓ / outlined ✕, caveat carries a real practical warning under a hairline). No manufactured changes.

### Discoveries / Gotchas
- **05-pickcard was NOT actually "good" — it had a live rendering bug Ray hadn't caught.** The DTP specimen (only one with real Amazon images) had tall bottle/spray PNGs ballooning ~167px out the bottom of the white image box and overlapping the persona/product/hook text. Root cause: `.qcard__img` is `display:grid; place-items:center; height:170px` but `overflow:visible`, and the img's default `min-height:auto` let it exceed the 142px content track. `min-height:0` lets `object-fit:contain` actually cap it (verified: img 141px, sits 15px inside box bottom). Did NOT use `overflow:hidden` — that would crop, violating "never cropped."
- **Same latent bug in 04-bottomline, but worse failure mode.** `.bl__img` is flex with `overflow:hidden`, so a tall PNG there would get *clipped* (not balloon). All file-1 specimens use placeholder spans so it was invisible in the mockup — but the real product PNG slots in later, and repo history (`audit-product-images.ps1`, the 605×1500 / 425×717 tall-bottle swaps on 2026-05-24) proves tall product images are a recurring real input. Fixed preemptively.
- **Dev server live-reload kept hijacking the agent-browser tab**, silently navigating it to `12-section-header.html?v=2` mid-session (caused one screenshot/eval mismatch where `get url` disagreed with what rendered). Fix: `agent-browser network route "**/livereload**" --abort` before opening — URL then held stable for measurement.
- **Library files are untracked** (`docs/playgrounds/library/` is `??` in git), so `git diff` has no baseline — couldn't use it for the hard-constraint verify. Verified frozen blocks intact by computed-style probes instead: gog CTA dark text `rgb(7,16,26)` + Press Start 2P product name capped 12px still active on file 2; all 4 changed lines confirmed by grep at the intended selectors only.

### Files Changed
- `docs/playgrounds/library/04-bottomline.html` — `.bl__inner` align stretch; new `.bl__main` flex-column space-between; `min-width/height:0` on `.bl__img img`.
- `docs/playgrounds/library/05-pickcard-glance.html` — `min-width/height:0` on `.qcard__img img` (fixes the DTP image overflow).

### Status at End of Session
- Both mockups verified per-skin by screenshot (gog = stress case for both). File 1 balance diff = 0 on all 5; file 2 DTP images contained 15px inside box. Frozen scaffold + 5 skin token blocks untouched. Files written in place, not committed (untracked playground).

---

## Library cohesion harmonization — applied approved audit findings across all 12 mockups

### What We Did
- Owner approved every finding in `docs/playgrounds/library/cohesion-audit.html` EXCEPT #4 (award banner vs pill — wants a real example first). Applied the rest as canonical primitives across the 12 mockup files, plus built the #4 decision aid `award-compare.html`.
- **#1 Card radius → 14px:** 05 at-a-glance qcard 6→14, 06 deep-card 12→14. Inner white image boxes → 10px: 04 `.bl__img` 9→10, 05 `.qcard__img` (none→10), 06 already 10 (kept), 11 `.qp-pick__img` 4→10. (04 `.bl__card` already 14 — left.)
- **#2 Buy button → one standard** (8px radius, 14px/700, 13×20 padding, resting `0 2px 9px rgba(0,0,0,.18)` + hover lift `0 6px 16px .22`): 03 hero `.btn`, 04 `.bl__buy` (was 9px/15px/14×18), 05 `.qcard__cta` (was 13.5px/12×16), 06 `.deep-card__rail__cta` (was 6px/13px uppercase — dropped uppercase), 08 `.cmp__cta` (was 11px/8×16 uppercase — dropped uppercase), 10 `.cta` showcase (was 15px/600/13×24), 11 `.qp-pick__buy` (was 2px/12px uppercase — dropped uppercase). Preserved each button's existing **background token** (06 `--forest`, 11 `--ink`, 08 `--brass`) and gog dark-text — those are skin colors, not the harmonized primitive.
- **#3 Eyebrow → canonical** (sans·12px·.16em·accent + 26×2 leading rule; gog override 9.5px/.08em): 04 `.bl__eyebrow` (was serif+brass+22px rule), 09 `.faq__eyebrow` (was centered, rule-less, 11px). 03 hero + 12 section-header already canonical (untouched).
- **#5 Card resting shadow → `0 2px 10px rgba(0,0,0,.06)`:** added to the flat cards 05 qcard, 06 deep-card, 07 guide-card + review-card; 04 `.bl__card` stepped DOWN from `0 6px 22px` to the canonical value (sits in-plane now).
- **#6 FussyBean `--accent` → `#b06f2c`:** fixed in 01 navbar, 02 footer, 03 hero (were `#d59b54`); added `--accent:#b06f2c` to fb skin in 10 CTA (was undefined). Left `--word-b`/`--footer-heading` (separate tokens) at `#d59b54`.
- **#7 gog `--brass-soft` → `#2c2752`** restored in 07 listing-cards (was overridden to `#9a93c7`); legibility fixed locally with `.skin--gog .review-card__stars { color: #9a93c7 }`.
- **Spacing → 4px grid:** 05 glance 30→32, 06 deep-wrap 30→32 + deep-card 26→24, 07 listing-stage 30→32, 08 cmp `30 30 34`→32, 11 qp `28 36`→32.
- **Built `award-compare.html`** — renders the harmonized deep-card twice (LEFT = current pill, RIGHT = at-a-glance-style full-width banner) in DTP + gog skins so the owner can pick which the deep card uses for #4.

### Decisions
- **Did NOT repaint button backgrounds.** In every skin `--brass`==`--accent` (identical hex), so the audit's NEW button using `--accent` was incidental, not an instruction. Buttons whose bg is genuinely different (`--forest` rail, `--ink` quick-picks) kept their token — repainting would be a banned skin-color change. (Advisor confirmed.)
- **Left file 12 + the editorial/hero paddings alone** (03 hero 56/48/52, 09 faq 48/54 vertical, 10 cta-stage 40/44, 12 gh 40/56). The spacing rule said "only where it doesn't fight a locked proportion" — these are deliberate. Also left all `.qp-pick` row internals (quick-picks row layout is explicitly locked).
- **FAQ eyebrow left-aligned, heading left centered** — faithful to the approved audit ("left-align with block"), but re-centering or re-aligning the heading was not requested. Flagged for owner (see below).

### Discoveries / Gotchas
- **Finding #7's stated rationale was wrong about WHICH element breaks.** The audit said restore `#2c2752` and recolor "the dark guide-card kicker." But in gog, `--forest:#ede9ff` is LIGHT, so the guide card is light → its kicker at `#2c2752` (dark) reads fine and needs no fix. The element that actually vanishes at `#2c2752` is the review-card EMPTY stars (`.review-card__stars` uses `--brass-soft` on `--paper-muted:#171331`, dark-on-dark). That's why someone overrode the token to lavender originally. Correct fix = restore token + local star recolor, NOT a guide-card kicker change (that would invert the bug). Advisor caught this; verified by screenshot.
- **Two faithful-but-looks-off executions to surface to owner** (NOT pre-decided): (1) **09 FAQ eyebrow** now left-aligned with the leading rule while the "Common questions" heading stays centered — visible left-eyebrow / centered-heading asymmetry. (2) **08 table CTA** jumped from a compact 11px/8×16 uppercase chip to the full 14px/13×20 button, which noticeably fattens table rows. Both are correct per the approved findings; owner should eyeball them.

### Verification
- Screenshot-checked the four advisor-flagged risk areas + general cohesion: gog 07 (review stars cyan + both kickers legible + cards now have resting shadow), 08 DTP table (standardized check-price button in-cell), 09 MWC FAQ (eyebrow rule + accent, alignment mismatch confirmed), 04 DTP/FB bottom-line (eyebrow converted to sans+accent+rule, card shadow softened, FB accent now caramel), 05 DTP at-a-glance (14px radius + shadow, award BANNER preserved untouched, pill price-badge intact), 06 DTP deep-card (14px radius + shadow, award PILL preserved untouched, rail button standardized, 24px padding).
- Confirmed finding #4 NOT touched: 05 banner and 06 pill render exactly as before. Locked layout proportions (column ratios, image heights, quick-picks row, bottom-line `1fr 248px`/1120/gap96) untouched.

### Files Changed
- `docs/playgrounds/library/01..11` — harmonization edits per finding above (12 section-header needed no change; already canonical eyebrow + locked editorial padding).
- `docs/playgrounds/library/award-compare.html` — NEW, the #4 pill-vs-banner decision aid (DTP + gog).

---
## Component-library rebuild (afternoon → evening, branch `rebuild/component-library`)

### What We Did
- **Front-end whack-a-mole → strategic pivot.** Ray flagged a cascade of layout issues across the cold sites (half-width prose, broken bullet-box lists, oversized/tall cards, lopsided guide headers, giant homepage featured images). Root cause: the buying-guide page is a 5× ~800-900-line bespoke per-site copy (reviews already share `ReviewArticle`; guides drifted). Decision (Ray): stop reactive patching — **standardize the pieces into one component library; every site = same components, skin + content only.**
- **Built a 12-component library as standalone 5-skin mockups** in `docs/playgrounds/library/` (nav, footer, hero, Bottom Line, at-a-glance pick card, deep card, listing cards, comparison table, FAQ, CTA, quick-picks row, section header) + a gallery `00-index.html`. Used CE design agents + parallel build agents. Iterated live with Ray component-by-component until he approved all 12.
- **Cohesion pass** (`cohesion-audit.html`): harmonized cross-component drift to canonical values (radius 14 cards / 10 img-box, button 8px+shadow, one card shadow, fb `--accent` #b06f2c fix, gog `--brass-soft` fix, spacing 32/20). Ray approved all findings except #4; chose **Banner** for #4 (deep-card award matches at-a-glance). Spec frozen + committed (`1d9f159`).
- **Rebuild kicked off on branch `rebuild/component-library`** (live sites untouched, by design): harmonized tokens added to `packages/shared-styles/src/tokens.css` (`2e04240`); token-map + content-data inventory + the two content forks recorded in `docs/brainstorms/2026-05-31-piece-library-spec.md`; **decisions sheet** `REBUILD-DECISIONS.html` built + opened for Ray; **DTP pilot subagent dispatched** to port the guide page through real components.

### Decisions
- **One shared component library, skin + content only per site** (Ray, locked). Rebuild ALL 5 sites + Magic Go on it. On a branch until Ray reviews/merges (my call — a 5-site rebuild going live unreviewed is too risky).
- **#4 deep-card award = full-width banner** (Ray chose, vs pill).
- **Proportions are sacred** (Ray, repeated) — the rendered page matching the approved mockup is the acceptance test, not "build green."
- **Two content forks deferred to Ray** (decisions sheet): (1) migrate Bottom Lines to buy/skip split vs graceful single-verdict; (2) award label auto-from-rank vs explicit field. Components built to support BOTH so the build isn't blocked.

### Discoveries / Gotchas
- Mockups use a flat invented token set (`--accent`/`--paper`/`--serif`); real codebase uses `--color-*`/`--space-*`/`--font-*`. Rebuild is a **token-mapping job, not copy-paste** (advisor trap #1). Map recorded in the spec doc.
- Content model is already rich — `products[]` carry image/affiliateUrl/priceFrom/bestFor/hook/facts/body — so most new-component data needs already exist; the only gaps are the two forks above (advisor trap #2 smaller than feared).
- Per-site skins already match the mockup skins (mockup values were pulled from each site.css), so token-mapped components auto-skin.
- Frozen-spec deltas to implement in REAL components (not re-mocked): deep-card banner (06 still shows old pill), compact table CTA, FAQ eyebrow/heading align.

### Status at End of Session
- Branch `rebuild/component-library`: tokens + frozen 12-mockup spec committed. DTP pilot porting in progress (background subagent).
- Awaiting Ray: the two decisions-sheet forks (open in browser). Live sites on `main` unchanged.
- Next: verify DTP pilot vs mockups → roll to other 4 sites → redo Magic Go to emit the new components → Ray reviews branch + merges.

---
## DTP guide-detail pilot — ported through real shared components (subagent)

### What We Did
- Rebuilt the DTP **buying-guide detail** page so it renders through real, token-mapped shared Astro components matching the frozen mockups. The bespoke ~809-line per-site `[...slug].astro` is now a ~55-line thin wrapper.
- **Built / updated in `packages/shared-ui/src/components/`:**
  - `BottomLine.astro` — added the 04 buy-card layout (Buy/Skip leads + circular ✓/✕ badges + accent eyebrow rule, right-rail buy-card with image+price+See-on-Amazon). Additive optional props (`buyIf`, `buyImage/buyPrice/buyHref`, `headsUp`); when absent it renders the ORIGINAL verdict-split layout unchanged, so every review page (all sites via ReviewArticle) is untouched. Verified: built review HTML still uses `.bottom-line`, guide HTML uses `.bl__buy`.
  - `GuideHeader.astro` — NEW. 12 full-width editorial header (kicker rule + edge-to-edge serif title + lede + "What's inside" facts panel [picks / price range min-max / best for] + byline row).
  - `QuickPickGrid.astro` — NEW. 05 at-a-glance 3-up grid with award BANNER on top, contain-on-white capped image, price pill, filled See-on-Amazon button + demoted review link.
  - `DeepPickCard.astro` — NEW. 06 deep card with the award rendered as the full-width top BANNER (award-compare RIGHT, the locked treatment — not 06's pill). Image col | body (persona, italic reason, product link, HTML prose) | price/facts rail.
  - `FaqList.astro` — NEW. 09 FAQ (centered accent eyebrow + serif heading, native `<details>` accordion open-by-default, +/− indicator). Fed from frontmatter `data.faq` — this was previously INVISIBLE on guide pages (same bug class as the old hidden BottomLine).
  - `GuideArticle.astro` — NEW. Assembles the page as full-bleed bands: hero → GuideHeader → BottomLine → "The Once-Over" (QuickPickGrid) → "The full detail" (DeepPickCards) → editorial `<Content/>` → FAQ → footer CTA. Computes isDraft, derives awards, price min/max, facts.
  - `ComparisonTable.astro` + `CTA.astro` — updated to the 08 compact-CTA / 10 button standard (8px radius, 14px/700, `--shadow-button`, `--color-accent-fg`).
- Registered the 5 new components in `shared-ui/package.json` exports.
- Rewired `sites/detailerpicks/.../[...slug].astro` → thin wrapper: computes `isDraft → robotsContent` + `bgTheme` for MainLayout, emits product JSON-LD, renders `<GuideArticle entry={entry} />`.
- `pnpm --filter detailerpicks build` GREEN (16 pages, all 6 guides + the review). Screenshot-verified every section against its mockup at ~1264px: header, buy-card, Once-Over grid, deep cards (banner), spec table, FAQ, footer. All match.

### Decisions
- **Section order = GuideHeader → BottomLine** (resolved the task's internal contradiction; matches the current page + BottomLine's "verdict under the title block" lock). Verdict-before-title reads wrong.
- **Award = banner, omitted when not derivable.** Only #1 ("Best Overall") and the lowest-price pick ("Best Value") get a banner; the other 4 picks render with no banner (we do NOT invent labels). Mockups show every card bannered, but the data only supports two awards — flagged for Ray.
- **Bottom Line fork: buyIf is TOP-LEVEL** (`data.buyIf.buy[]`/`skip[]`), not under bottomLine, and there is **no `headsUp` field** in the schema — so the "Heads up" caveat is omitted on this content (the prop exists for future use). buy/skip are arrays (4/3 items) → rendered as tight bullet lists under the Buy/Skip leads.
- **Ported the editorial `.prose` CSS into GuideArticle** (spec table, chunked-bullet panels, h2 dividers) — these live in the markdown `<Content/>` body, NOT in ComparisonTable, so updating that component does not cover them. Dropping the port would regress the body hard.

### Discoveries / Gotchas
- **Latent build-breaker in `packages/shared-styles/src/tokens.css` (committed `2e04240`, never built).** The cohesion-pass comment contained `--color-*/--font-*/--space-*` — the `*/` sequence **closed the CSS comment early**, exposing garbage tokens; PostCSS threw `Unknown word tokens` at 158:62. Fixed by rewording to `--color- / --font- / --space-`. This bit the very first build; the tokens file had never been compiled since that commit. (Same `*/`-in-comment hazard also hit my `GuideHeader.astro` JS frontmatter comment — fixed there too.)
- **agent-browser on Windows has no `resize`/`viewport` subcommand** — used `eval(window.scrollTo/innerWidth)` for section captures; default viewport 1264px sits right at the SiteShell panel's 1240px cap, so the guide renders at full design width without resizing.
- The DTP P&S Xpress and the budget Meguiar's quick-card images are dark marketing **composites** (not clean product PNGs) — a content/image-quality issue (the kind `audit-product-images.ps1` catches), NOT a layout bug. Containment is correct; the source image is just busy.
- **Roll-out follow-ups (all 5 sites incl. DTP, since the pilot wrapper set the pattern):** (1) **FAQPage JSON-LD is no longer emitted** on guide pages — the bespoke pages built an inline `faqLd` script; the shared `GuideArticle`/`FaqList` render the visible FAQ but no schema. Hoist the FAQPage JSON-LD into `FaqList.astro` so all 5 sites get it uniformly. (2) **`ogImage` dropped** from the guide MainLayout call (bespoke passed `data.images?.hero`); DTP's pilot also omits it. Restore via the wrapper or a shared head once the pattern is decided. Both are SEO regressions, neither blocks render/build.
- **`GuideArticle` accepts a `crumbHref` prop but never forwards it to `GuideHeader`** (lines ~162–172) — so the "← All buying guides" breadcrumb LINK doesn't render on any site (DTP included). Can't restore from the wrapper without editing the shared component (out of scope this task). Noted for the component pass.
- **Background `astro preview` is cwd-fragile under the Bash tool:** `cd <subdir> && astro preview` background commands exited 127 (cwd resets between Bash calls), and stale preview servers from a prior session were still bound to 4331–4334 serving the WRONG sites — which produced misleading 404s/wrong-title results until I killed all node processes and restarted with `pnpm --filter <site> exec astro preview --port <p>` (filter is cwd-independent). Lesson: launch previews via `pnpm --filter`, and verify port→site by curling `<title>` before screenshotting.

### Files Changed
- `packages/shared-ui/src/components/` — `BottomLine.astro` (buy-card layout, backward-compatible), `GuideHeader.astro` (new), `QuickPickGrid.astro` (new), `DeepPickCard.astro` (new), `FaqList.astro` (new), `GuideArticle.astro` (new), `ComparisonTable.astro` + `CTA.astro` (button/CTA standard).
- `packages/shared-ui/package.json` — exports for the 5 new components.
- `packages/shared-styles/src/tokens.css` — fixed the comment `*/` that broke the build.
- `sites/detailerpicks/src/pages/buyers-guides/[...slug].astro` — 809-line bespoke page → thin `<GuideArticle>` wrapper.
- `sites/{mywildlifecam,fussybean,gameovergear,starteraquarium}/src/pages/buyers-guides/[...slug].astro` — each bespoke ~800-line page → thin `<GuideArticle>` wrapper (`b1e38f0`). +48/−3153 total.

### Status (pilot + roll-out)
- DTP guide-detail pilot DONE on the branch, builds green, screenshot-matches the mockups. Reviews untouched (verified).
- **Roll-out to the other 4 sites DONE (`b1e38f0`).** All 4 build green, all 4 screenshot-verified (header/facts/buy-card/Once-Over grid/deep cards/FAQ/footer render, skins correct). Reviews, shared components, Magic Go, DTP, and main all untouched.
- Gaps to flag to Ray: (1) only 2 of 6 picks get an award banner from rank/price derivation; mockups show all bannered — either accept partial banners or add explicit `award` fields. (2) a couple DTP product images are dark composites (image-audit candidates, not layout). (3) FAQPage JSON-LD + ogImage no longer emitted on guide pages (all 5 sites) — hoist into shared `FaqList`/head. (4) `crumbHref` not forwarded by `GuideArticle` → breadcrumb link absent on all guides. (5) Several GOG guides + one SA guide have no `faq:` frontmatter → no FAQ section renders (correct graceful degradation, not a bug).

---
## Rebuild execution (branch `rebuild/component-library`)
- **Tokens** (`2e04240`): harmonized primitives added to shared-styles (radius-card 14, radius-button 8, shadow-card/button, pad aliases). Caught later: a `*/` inside a token comment closed the CSS comment early → would break the build; reworded (it had never been compiled since commit).
- **Spec frozen** (`1d9f159`): 12 mockups committed as the visual acceptance test + token map + content-data inventory + frozen-spec deltas in `docs/brainstorms/2026-05-31-piece-library-spec.md`.
- **DTP pilot** (`dd64908` + `d443008`): new shared components built token-mapped to the mockups — `GuideHeader`, `QuickPickGrid`, `DeepPickCard`, `FaqList`, `GuideArticle`; updated `BottomLine` (additive buy-card props — reviews untouched), `CTA`, `ComparisonTable`. DTP `buyers-guides/[...slug].astro` → ~55-line thin wrapper. Build green. **Self-screenshot-verified vs mockups** (header+facts panel, Bottom Line buy-card, Once-Over grid, deep cards w/ banners, table, FAQ). Award fix: banner every card via `bestFor` fallback (no persona dup).
- **4-site roll** (`b1e38f0`): mwc/fussybean/gog/sa guide pages → thin GuideArticle wrappers (−3153 lines of bespoke code). All 4 build green; agent screenshot- + curl-verified per skin. Broken cold-site guide symptoms (half-width prose, broken bullet boxes, lopsided headers) fixed by routing through the verified components.
- **Regression fixes** (`c60f5c3`): restored FAQPage JSON-LD + breadcrumb forwarding in shared GuideArticle (verified in built HTML on DTP + gog).

### Verified
- All 5 sites build green. Guide-detail pages render through the shared components. DTP self-verified; 4 cold sites agent+curl-verified. FAQPage + breadcrumb present in emitted HTML.
- **Caveat:** Playwright/Chrome MCP disconnected late in the session — could not self-screenshot the final cold-site states (relied on the roll agent's screenshots + curl/build). Re-verify visually next block.

### Remaining (next block)
- **Homepages** (esp. bespoke FB/GOG/SA) → route through shared Hero + cards. Needs visual verification.
- **Listing indexes** — confirm/align to shared cards.
- **Magic Go** — lighter than feared: scaffolder already emits the products[] frontmatter GuideArticle consumes. Real change = the two fork fields once Ray decides.
- **og:image** per-wrapper restore (SEO follow-up).
- **Decisions for Ray** (open in `REBUILD-DECISIONS.html`): (1) Bottom Line buy/skip migration; (2) award label auto-vs-field. Plus the per-site "quick scout" puns (DTP = The Once-Over chosen).

### Status at End of Block
- Branch `rebuild/component-library` (commits 2e04240 → c60f5c3): library + all 5 guide pages rebuilt + green + regressions fixed. `main` + live sites untouched. Ray reviews branch + answers 2 forks → next block does homepages/indexes/Magic Go with screenshots back.
