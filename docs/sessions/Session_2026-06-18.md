# Session — 2026-06-18

## What We Did
- Grounded review of the whole affiliate-kit (triggered by an external AI "Kimi" review Ray pasted in). Fact-checked Kimi's claims against the live repo — its entire "Critical" tier was stale/wrong (DTP tag IS set = `detailerpicks-20`; all 5 sites tagged; 0 `draft:true` pieces — the 38-draft queue was already flipped live in `1a26389`; the "hands-on" template "violation" is actually doctrine-COMPLIANT negation copy in `how-we-evaluate.astro`).
- Ran 3 parallel Explore agents: strategic state, script-usage audit, SEO/indexing posture.

- Built + verified **`scripts/smoke-cloaker.ps1`** — live smoke-test of every `/go/<slug>` route against production (closes the gap where `check-dead-links.ps1` skips `/go/*`). Read-only (curl, no wrangler). Derives origin from `dist/sitemap-index.xml`, scans built HTML for `/go/` hrefs, expects 301/302, exits 1 on any 404. First run reproduced all 24 breaks exactly (MWC 12/12 pass). Both new scripts NOT yet committed.

- **Delivered `C:\Users\rchampion\Desktop\affiliate-kit-fix-at-home.html`** — self-contained at-home checklist (matches request-indexing-checklist.html style: dark theme, copy buttons, localStorage progress). Contains: Step 1 = all 24 `add-link` fix commands (copy-all + per-row); Step 2 = full `smoke-cloaker.ps1` source to save+commit at home + verify; Step 3 = cleanup (rm orphan `magic-go-setup-batch.ps1`, refresh stale TODO verdict-queue section, "keep these" note on Kimi's bad cut targets); Step 4 = deferred decisions (MWC→satellite links WITH cross-niche/link-network caveat, click-query bundle, pillar IA on money sites). Self-contained because home = different machine (basement PC) than this Arrow work box — uses already-committed add-link.ps1, no dependency on the un-pushed scripts here.
- **Did NOT commit/push** (respecting "commit only when asked" + avoiding an unrequested 5-site auto-deploy). The fix + guard scripts written here (`scripts/fix-broken-cloaker-links-2026-06-18.ps1`, `scripts/smoke-cloaker.ps1`) sit untracked on the work machine; their real delivery vehicle is the embedded source in the desktop HTML.

- **FIXED LIVE (not deferred to home).** Ray authed wrangler on the work machine (`wrangler login` OAuth, token has `workers_kv:write`). Wrote all 24 missing KV keys directly via the workspace wrangler binary (`workers/link-cloaker/node_modules/.bin/wrangler`, v3.114.17), namespace `83ca8a76111b47899caba8670c48dfbc`. **Final verify: `smoke-cloaker.ps1` → PASS, all 70 cloaker routes across all 5 sites resolve 302.** The portfolio-wide affiliate leak is closed.
  - **Gotcha 1:** `add-link.ps1` hardcodes `--remote`, which wrangler 3.114 REJECTS (`Unknown argument: remote` — in this version remote is the default, `--local` is the opt-out). Bypassed the script and called `wrangler kv key put` directly (no `--remote`). **The committed `add-link.ps1` is broken against modern wrangler — needs the `--remote` flag removed (or made version-conditional).**
  - **Gotcha 2:** first `kv key put` hit a transient `/memberships` Authentication error 10000 right after token mint; retry succeeded. KV edge propagation took ~20–40s before routes flipped 302 consistently (expected).

## Discoveries / Gotchas
- **24 BROKEN affiliate links across 4 of 5 sites (portfolio-wide revenue leak).** Started checking DTP (where Ray noticed), but advisor flagged it as a process gap not a DTP quirk → smoke-tested all 5 sites' `/go/` routes against production. Result: **MWC clean (0 broken)**, DTP 4, fussybean 9, starteraquarium 7, gameovergear 4 = **24 dead routes, ALL single-product `*-review` pages** (+ `baratza-encore-esp`). Buyers-guides + quick-picks resolve everywhere; MWC reviews all registered. Root cause: the `add-link.ps1` KV-registration step was never run for satellite + DTP review pages (only MWC's reviews + everyone's guides/quickpicks got keys). Every "Check price on Amazon" button on those 24 pages 404s — the highest-intent clicks in the portfolio. Destinations pulled from each review's `affiliate.amazon` frontmatter (correct per-site tags), saved as runnable fix: **`scripts/fix-broken-cloaker-links-2026-06-18.ps1`** (needs wrangler authed → run on basement PC; this clone has no wrangler).
- **Systemic gap:** `check-dead-links.ps1` deliberately EXCLUDES `/go/*`, so affiliate links — the highest-revenue links — have ZERO automated guard. The 4 breaks were only caught because Ray happened to notice. The live `/go/` smoke-test should be scripted.
- **Kimi's cut-list is mostly wrong:** `install-desktop-shortcut`, `keyword-suggest`, `check-dead-links`, `fix-product-images`, `pick-square-image` are all actively referenced (package.json / magic-go.md / pre-commit / sessions). Genuine orphans: `list-links.ps1`, `remove-link.ps1`, `magic-go-setup-batch.ps1` — and list/remove-link are the useful manual pair to add-link, so really only `magic-go-setup-batch.ps1` is a clean cut candidate.
- This clone had no `node_modules` — ran `corepack pnpm install` (13.7s) to build DTP for the dead-link scan. Internal links: clean (0 dead).

---

## Session End Safety Net — 2026-06-18 12:30:40

_Auto-appended by SessionEnd hook. Captures git activity only — NOT decisions, gotchas, or reasoning. Run `/wrap` during the session for the full record._

### Working tree at session end

```
 M docs/sessions/Session_2026-06-12.md
?? docs/brainstorms/2026-05-28-steal-from-aaron.md
?? docs/request-indexing-checklist.html
```

### Uncommitted diff stat

```
 docs/sessions/Session_2026-06-12.md | 27 +++++++++++++++++++++++++++
 1 file changed, 27 insertions(+)
```

### Untracked files

```
docs/brainstorms/2026-05-28-steal-from-aaron.md
docs/request-indexing-checklist.html
```

